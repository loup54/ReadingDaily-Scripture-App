# ReadingDaily Scripture App — Todo List

## Pending

### Type-check debt (found 2026-08-15, `verify.sh` added — see git log `7b0e868`)
`npx tsc --noEmit` went from ~785 to 649 errors after fixing an unrelated parse bug that had been masking the real count, plus a jest-native matcher type gap (git log `e4a3676`). Remaining, parked, not urgent (doesn't block builds/OTA — RN doesn't enforce types at runtime):
- [x] Theme key mismatches (129 errors, 10 files) — fixed 2026-08-16, commit `1ee9d60`. Mapped each bad key to the correct existing token per-file (`.title`→`.h1`/`.h2`/`.h3` by context, `.main`→`.blue`/`.primary`, flat `colors.background/.text` → nested `colors.background.primary`/`colors.text.primary` shape, etc.) — see commit for full mapping. `tsc --noEmit` total: 649 → 520. Some of these files still have unrelated errors left (missing props, unrelated type mismatches) — out of scope for this fix, folded into the "Remaining ~510" bucket below.
- [x] `container` destructured from `render()` in ~45 test errors — fixed 2026-08-16. Swapped `const { container } = render(...)` / `expect(container).toBeTruthy()` for RNTL's `toJSON()` (`const { toJSON } = render(...)` / `expect(toJSON()).toBeTruthy()`) in `src/components/audio/__tests__/HighlightedTextDisplay.test.tsx` (16→0) and `src/components/audio/__tests__/HighlightedReadingPlayer.e2e.test.tsx` (31→0, also renamed the deprecated `getAllByA11yRole` query to `getAllByRole`). Both files now fully type-clean. `tsc --noEmit` total: 520 → ~499 after this piece alone.
- [x] Service mocks vs real class shape drift — partially fixed 2026-08-16, ~21 of the ~44 estimated errors. Investigation found the estimate undercounted: the named symptom (`getInstance`/`getUserAcceptances`/`getAllDocuments` missing) is only the visible tip in two files that turned out to be much more broadly out of sync with their real service (`DocumentVersioningService.test.ts`, 44 errors total, and `IntegrationTests.test.ts`, 19 errors total) — those two invent method names/signatures (`getDocumentVersion`, `getCurrentVersion`, `getAllDocuments`, `getUserAcceptances(userId)`, `isDocumentAccepted(docId, userId)`, a 5-arg `recordAcceptance`) that don't exist under any name on the real static-only `DocumentVersioningService`, so fixing them needs real behavioral decisions (which real method replaces each fake one), not a mechanical rename — left untouched, folded into the untriaged bucket below rather than guessed at blind (jest itself can't run in this environment right now, see item below, so there's no way to verify a guessed fix). What *was* safely fixable (real class confirmed static-only or auto-mock confirmed to already cover the real shape, fix is a pure reference-pattern correction with no invented behavior): `src/services/legal/__tests__/DocumentSigningService.test.ts` — real `DocumentSigningService` is static-only (no `getInstance`); rebound the test's `service` variable straight to the class (`typeof DocumentSigningService`) instead of a fake singleton instance, which resolved every downstream `service.captureSignature(...)`-style call in one shot (61→41 errors; the remaining 41 were a separate, real drift — `CapturedSignature` dropped the `'sketch'` signature type and gained required `timestamp`/`device` fields the tests didn't pass, `verificationHash` was renamed to nested `verification.hash`, `SignatureExport` lost a `count` field. **Resolved 2026-08-16**: confirmed intentional via the explicit code comment in `SignatureModal.tsx` (`type SignatureMode = 'typed'; // Removed 'choose' and 'sketch' - typed name only`) — sketch-drawn capture was deliberately removed from the UI, typed-name-only is the current, correct design; the STORED `DocumentSignature.signatureType` still supports `'sketch' | 'typed'` for legacy records. Updated the test file to match: object literals typed as `CapturedSignature` and passed INTO `captureSignature()` now use `type: 'typed'` plus the newly-required `timestamp`/`device` fields; `DocumentSignature` record literals that test stored/legacy signatures (not new captures via `captureSignature()`) correctly kept `signatureType: 'sketch'` since the stored type still allows it (3 such literals, also needed `verification: {...}`/`syncStatus` added since they were missing those too); `verificationHash` references → nested `verification.hash`; `.count` → `.signatures.length`. File's 41 tsc errors → 0; project total 410 → 369. `npm test` for this file still has 14 runtime failures, but all are pre-existing logic/mock-setup mismatches unrelated to this type fix (same category as the untriaged mock drift elsewhere in this section) — e.g. `clearSignatures` test expects `AsyncStorage.multiRemove` but real code loops `removeItem`; several retrieval/hasSigned tests mock `AsyncStorage.getItem` with full signature objects when the real signature-index format expects an array of ID strings (`signatureIds is not iterable`); a few tests assert `.rejects.toThrow()` for inputs the real code doesn't validate (empty signature data, oversized data, missing document ID) and it just resolves instead; platform-metadata tests expect `metadata.platform` to equal the function's `platform` argument, but the real (confirmed-correct, untouched) source assigns `metadata.platform = signature.device` and `metadata.device = platform` — left untouched, not part of this fix's scope. `src/components/legal/__tests__/LegalDocumentViewer.test.tsx` — `DocumentAnalyticsService.getInstance` was called on a class that's static-only and auto-mocked by `jest.mock(...)`; rewired the test's `mockAnalyticsService` alias to point at the auto-mocked static methods directly instead of a disconnected mock object nobody ever wired up (4→3 errors; the file's other 3 errors are an unrelated `LegalDocument.sections` shape mismatch). `ComplianceAnalyticsScreen.test.tsx` (`src/screens/legal/`) and `NotificationService.test.ts` (`src/services/notifications/`) were left alone even though they were named in the original estimate — both live in directories explicitly called out below as needing individual per-file review, and `NotificationService.test.ts` in particular has 18 other unrelated errors (wrong `PushNotification`/`NotificationStats`/`DailyReminder`/`NotificationFilter` shapes, a `@/services/firebase/config` import that doesn't resolve) beyond the one `getInstance` mismatch. `tsc --noEmit` total for this bucket: ~499 → 452.
- [x] `logger.*(message, ...extraArgs)` calls passing a second/third/fourth argument that isn't an `Error` — fixed 2026-08-16. `LoggerService`'s real signature is `debug/info/warn/error(message: string, error?: Error): void` (see `src/services/logging/LoggerService.ts`), but ~32 call sites across `src/services/notifications/NotificationService.ts` (28) and `src/services/notifications/DailyReminderScheduler.ts` (4) passed a plain string/id or a context object literal as a second argument, expecting it to be interpolated — TS correctly rejected these (`Argument of type 'string' is not assignable to parameter of type 'Error'`, `Object literal may only specify known properties, and 'id' does not exist in type 'Error'`, `Expected 1-2 arguments, but got 3/4`). Folded every extra argument into the message via template literals (objects via `JSON.stringify`) so the exact same information still reaches the log/Sentry breadcrumb, no logging behavior changed, `LoggerService` itself untouched. `tsc --noEmit` total: 452 → 420. `npm test`: still 291 pass / 7 fail (unchanged — same pre-existing content mismatches, confirms no regression from this pass; the two touched files' own test suites can't currently run at all, blocked by unrelated pre-existing `Cannot find module` errors noted in the item above).
- [ ] Remaining ~369 errors, mostly `src/screens/legal/`, `src/screens/subscription/`, `src/services/payment/`, plus the two service-mock test files identified above (`src/services/legal/__tests__/DocumentVersioningService.test.ts`, `src/services/legal/__tests__/IntegrationTests.test.ts`) — untriaged, real per-file review needed. (`DocumentSigningService.test.ts`'s `CapturedSignature`/`verificationHash` drift was resolved 2026-08-16 — see item 9 above.)
- [x] `jest-expo` pinned at `~52.0.6` vs Expo `~54` mismatch — fixed 2026-08-16, commit `dee9d24`. Root peer conflict was `@types/react` (`~18.3.12`) vs what `react-native@0.81.5` actually requires (`^19.1.0`, matching installed `react@19.1.0`) — bumped `@types/react` to `~19.1.10` and `jest-expo` to `~54.0.17` (both per `npx expo install --check`'s recommendation). `npm install`'s peer graph was already broken pre-existing for an unrelated reason (`react-native-iap` needs `react-native-nitro-modules@^0.31.1` vs the pinned `^0.29.8` — confirmed via `git stash` + fresh `npm install` that this predates this fix), so used `--legacy-peer-deps`, matching how the repo's existing `node_modules` were already assembled; left the nitro-modules/IAP version mismatch untouched (native/payment surface, out of scope). Result: `jest-expo`'s setup no longer crashes — `npm test` now actually executes 298 tests across 31 suites: **291 pass**, 7 fail on pre-existing content/behavior mismatches unrelated to this fix (TTS locale codes in `wordPronunciation.test.ts`, sentence-length/difficulty thresholds in `SentenceExtractionService.test.ts`, calendar/loading-state text assertions in `ReadingCalendar.test.tsx`/`ProgressDashboard.test.tsx`). 19 suites still fail outright on newly-surfaced (couldn't be seen before since nothing ran) infra issues, left untriaged: 12 suites hit `[@RNC/AsyncStorage]: NativeModule: AsyncStorage is null` (jest-expo 54's preset no longer auto-mocks it the way 52 did, or mock wiring needs updating), 3 hit `Jest encountered an unexpected token` (transform config, all in `src/services/audio/__tests__/` + `HighlightedReadingPlayer.e2e.test.tsx`), 3 hit `Invariant Violation: TurboModuleRegistry.getEnforcing(...): 'DevMenu' could not be found` (all `src/screens/legal/` + `src/components/legal/__tests__/LegalDocumentViewer.test.tsx`, tests that manually `jest.mock('react-native', ...)` and call `jest.requireActual`), and 1 hits `Cannot find module '@react-native-firebase/messaging'` (`FirebaseCloudMessagingService.test.ts` — package genuinely not installed). `NotificationService.test.ts`'s `Cannot find module '../../firebase/config'` failure is not new — already flagged in item 9 above. `tsc --noEmit`: 452 → 452 (unchanged, confirms the `@types/react` major bump introduced no new type errors).
- [x] The 7 pre-existing content/behavior mismatches named in item 12 above (`wordPronunciation.test.ts` x3, `SentenceExtractionService.test.ts` x2, `ReadingCalendar.test.tsx` x1, `ProgressDashboard.test.tsx` x1) — fixed 2026-08-16. All 7 were **test-only fixes**, not real-code bugs — in every case the real `src/` behavior was confirmed intentional/correct and the test was stale:
  - `wordPronunciation.test.ts`: `getTTSLanguageCode('zh-CN'/'zh-TW'/'ar')` expected the raw translation-service codes back unchanged (`zh-CN`, `zh-TW`, `ar-SA`); the real map deliberately converts to actual Google Cloud TTS locale codes (`cmn-CN` Mandarin, `yue-HK` Cantonese, `ar-XA` Arabic — all documented Google Cloud TTS conventions, with comments in `src/utils/wordPronunciation.ts` explicitly calling this out). Also `validatePronunciation('the', ...)`'s reason string expected to contain `'stop word'`, but the real message is the user-facing `'Common English word (no pronunciation needed)'` (a separate, more technical `'stop word'` wording exists in `getPronunciationReason()` in `stopWords.ts` and is tested/passes separately) — updated the assertion to match the real user-facing copy.
  - `SentenceExtractionService.test.ts`: both failures traced to the same commit (`f6eed73`) that deliberately tuned `MAX_WORDS` 30→100 ("allows longer scripture verses") and added clause-separator splitting (commas/semicolons/colons, not just `.!?`) to `splitIntoSentences` ("scripture often uses semicolons, colons, commas") — confirmed via `git log -p` this was an intentional design change, not a regression, bundled into a commit literally titled "Fixed arrow navigation in pronunciation practice (sentence extraction word limits)". The `validateSentences` "too long" test used `wordCount: 30`, which no longer exceeds the new 100-word ceiling — bumped to 150. The "complex punctuation" difficulty test used a heavily comma/colon-laden sentence which the new clause-splitting logic now breaks into several short, easy fragments before the difficulty scorer ever sees the punctuation density — replaced with a longer quoted sentence with no internal clause separators, so it survives as one chunk and still exercises the word-count + quotation-mark difficulty factors.
  - `ReadingCalendar.test.tsx`: the "100% consistency" test hardcoded reading data for November 2025, but `ReadingCalendar` has no prop to pin its displayed month — it always defaults to the real current date (`useState(new Date())`), which is correct/intentional behavior for a "today-centered" calendar. The test only passed when originally written near Nov 2025; as real time moved on, none of the hardcoded dates matched the actual displayed month, so consistency computed as 0%. Fixed the test to generate its full-month reading data from the actual current year/month at run time instead of a hardcoded month.
  - `ProgressDashboard.test.tsx`: the loading-state test queried for text matching `/Loading/i`, but the loading branch (`src/screens/progress/ProgressDashboard.tsx:184-189`) renders `<DashboardSkeleton />` — an animated shimmer-placeholder skeleton with no text at all (a deliberate, more polished UX pattern than a bare "Loading..." string). Updated the test to assert the skeleton component itself via `UNSAFE_getByType(DashboardSkeleton)` instead of querying for text that no longer exists.
  - No `src/` application code was changed for any of the 7 — all fixes confined to the 4 test files. Verified in isolation (`npm test -- <file>`): all 4 suites, 140/140 tests pass.

  **AsyncStorage + unexpected-token clusters fixed 2026-08-16, commit `03ec2a5`.** AsyncStorage (12 suites): `@react-native-async-storage/async-storage`'s bundled Jest mock (`jest/async-storage-mock.js`) doesn't self-register via `jest.mock()` in the installed version — it's just a plain mock object export, so it has to be wired in explicitly. Added `jest.setup.js` (registered via `"setupFiles"` in `package.json`'s `jest` field) doing `jest.mock('@react-native-async-storage/async-storage', () => require('.../jest/async-storage-mock'))`. Unexpected token (3 suites, all `src/services/audio/__tests__/*` + `HighlightedReadingPlayer.e2e.test.tsx`): root cause was the `firebase` npm package's ESM build — jest-expo's default `transformIgnorePatterns` allowlist (react-native/expo/navigation/sentry/native-base) doesn't include `firebase`/`@firebase`, so babel never transformed its ESM syntax. Added `firebase`/`@firebase` to `transformIgnorePatterns` in `package.json` (full pattern copied forward from jest-expo's preset, since preset transformIgnorePatterns/transform are overridden-not-merged by user config — confirmed via `jest-config`'s `normalize.js`). That surfaced a second, narrower gap: jest-expo's own transform config only matches `\.[jt]sx?$`, but the newly-unignored `@firebase/util/dist/postinstall.mjs` is a `.mjs` file — added an explicit `"\\.mjs$": "babel-jest"` transform entry (this one *does* merge with the preset's transform map). Result: all 15 target suites now execute past module load — zero remaining "Test suite failed to run" for AsyncStorage or unexpected-token causes. Two are fully green (`Button.test.tsx`, `PracticeStorageService.test.ts`); the rest fail on real, pre-existing content/behavior/mock-drift issues unrelated to this fix (the `getInstance`-on-static-class drift from item 9 above turned out to also hit `BackupServices.test.ts`, not just the two files originally named there). `npm test`: 24 failed/7 passed suites → 22 failed/9 passed (31 total); 291 pass/7 fail/298 total tests → 490 pass/282 fail/772 total (many more tests now execute since previously-crashing suites run their full content instead of 0 tests). `tsc --noEmit`: 420 → 420, unchanged. One newly-exposed issue is a **real application bug**, not infra, and was left untouched per this fix's scope: `src/hooks/useAudioPosition.ts:42` calls `React.useState(...)` but the file only imports `{ useEffect, useRef, useCallback }` from `'react'`, never the `React` default export — `HighlightedReadingPlayer.e2e.test.tsx` fails with `ReferenceError: React is not defined` because of it. Needs a one-line `import React from 'react'` fix, but that's a `src/` source change, out of scope here. Remaining untriaged: 3 `DevMenu` suites (`src/screens/legal/` + `LegalDocumentViewer.test.tsx`) — not started.

- [x] `useAudioPosition.ts:42` missing `React` import — fixed 2026-08-16, commit `1416b12`. Called `React.useState(...)` without importing the `React` default export; invisible while the jest-expo parse crash masked it. `tsc --noEmit`: 420 → 417. `HighlightedReadingPlayer.e2e.test.tsx`: `React is not defined` crash gone, 33/34 tests in that suite now pass.
- [x] Firebase Cloud Messaging feature — **removed** 2026-08-16, commit `f680256`, resolving the `@react-native-firebase/messaging` missing-module suite (no longer applicable, file deleted). Investigated before deciding: client-side listener code (`FirebaseCloudMessagingService.ts`, `FCMTokenManager.ts`, `useFCMNotifications.ts`) was fully built (Phase 10B.7) but never wired in — `app/_layout.tsx` had it commented out pending the native package install that never happened. Checked for server-side sending code (`functions/`, `python-functions/`) — **none exists**, no Cloud Function anywhere calls Firebase Admin's messaging API, so even installing the client package wouldn't have produced a working feature, just a listener with nothing to listen for. Decided the actual UX case (server-triggered notifications deep-linking to a specific passage) isn't worth building now — the existing local `NotificationService` already covers the core daily-reminder need, and there's no concrete driver (re-engagement campaign, content-correction alerts, personalization) to justify a native rebuild + building server infra from scratch. Removed all three files (1,484 lines) plus the dead commented-out wiring in `app/_layout.tsx`. Revisit only alongside a real use case, not as standalone infra.
- [x] `DevMenu` invariant violation (3 suites: `src/screens/legal/__tests__/BackupExportScreen.test.tsx`, `src/screens/legal/__tests__/ComplianceAnalyticsScreen.test.tsx`, `src/components/legal/__tests__/LegalDocumentViewer.test.tsx`) — fixed 2026-08-16. Root cause traced to `react-native`'s own `index.js`: `DevMenu` (and `DevSettings`) are exposed as **lazy getters** (`get DevMenu() { return require('./src/private/devsupport/devmenu/DevMenu').default; }`), only evaluated if the property is actually accessed — under jest-expo's normal preset mock they're never touched, so no problem. All 3 test files instead did `jest.mock('react-native', () => ({ ...jest.requireActual('react-native'), Alert: { alert: jest.fn() } }))` (LegalDocumentViewer also spread in `Share`) — the `{...jest.requireActual('react-native')}` **object spread eagerly enumerates every property on the real module**, including the `DevMenu` getter, which triggers `require('.../DevMenu').default`, which calls `TurboModuleRegistry.getEnforcing('DevMenu')` — and since Jest's test environment has no native `DevMenu` module registered (nothing mocks it, because nothing normally touches it), that throws the Invariant Violation. This is new/newly-surfaced under jest-expo 54, not a bug in the mock itself going stale — RN 0.81's `index.js` getter-based export shape is what makes a plain spread dangerous; earlier RN/jest-expo versions the tests were written against likely didn't expose `DevMenu` this way (or the preset shape at spread time was already a flattened, non-getter mock object). None of the 3 files actually needed the real `react-native` module at all — each only wanted `Alert.alert` (and `Share.share` for `LegalDocumentViewer`) to be a spy-able `jest.fn()`; every other RN import (`View`, `Text`, etc.) already resolves fine through jest-expo's default preset mock, same as the other 27 suites in the project that don't touch `react-native` in their own `jest.mock()` at all. Fix: deleted the `jest.mock('react-native', ...)` block in all 3 files entirely and replaced it with a narrow `jest.spyOn(Alert, 'alert').mockImplementation(() => {})` (+ `jest.spyOn(Share, 'share').mockResolvedValue(...)` in `LegalDocumentViewer.test.tsx`) — `Alert`/`Share` were already imported normally from `'react-native'` at the top of each file, so no import changes needed. `jest.clearAllMocks()` in each file's existing `beforeEach` resets call history but not the spy implementation, so this persists correctly across tests. Verified no `DevMenu`/`TurboModuleRegistry` string appears anywhere in output after the fix. Result: all 3 suites now execute past module load (previously 0 tests ran per suite); they still fail on real, pre-existing content/mock-drift issues unrelated to this fix — `BackupExportScreen.test.tsx` 37 pass/10 fail (47 total), `ComplianceAnalyticsScreen.test.tsx` 0 pass/43 fail (all fail on `DocumentAnalyticsService.getInstance` being `undefined` — this is the *exact* `getInstance`-on-static-class drift already named and explicitly left untouched for this file in item 9 above), `LegalDocumentViewer.test.tsx` 31 pass/17 fail (48 total, mix of `getByRole` accessible-name and duplicate-text query issues). `npm test`: 17 failed/13 passed suites → 17 failed/13 passed suites (unchanged — all 3 suites still have real content failures, same pattern as the AsyncStorage/unexpected-token fix above), 240 failed/532 passed/772 total tests → 309 failed/601 passed/910 total tests (+138 tests now execute since the 3 suites run their full content instead of 0; +69 pass, +69 fail — the fail increase is 100% pre-existing content drift, none of it new). `tsc --noEmit`: 369 → 369, unchanged (confirms this was pure jest-config/mock surgery, no type-level change). Only the 3 test files were touched — no `src/` application code changed. **This closes out today's jest-expo 52→54 migration infra cluster — every "suite failed to run" class of failure (AsyncStorage, unexpected-token, DevMenu, FCM missing-module) is now resolved; everything left failing is real per-file content/behavior/mock drift.**

**Honest snapshot as of 2026-08-16, end of jest-expo migration session** (all numbers from a fresh `npm test` / `npx tsc --noEmit` run, not carried forward from earlier bullets):
- `tsc --noEmit`: **369 errors**, unchanged since the `DocumentSigningService.test.ts` fix earlier today (item 9 above) — nothing done since then was type-level work.
- `npm test`: **17 failed / 13 passed suites (30 total)**; **309 failed / 601 passed tests (910 total)**.
- Zero suites remain in the "won't even execute" category (no more `NativeModule is null`, `unexpected token`, `DevMenu`, or `Cannot find module '@react-native-firebase/messaging'` crashes). Every failing suite now fails on real content/behavior/mock-drift, i.e. genuine per-file test-vs-implementation review, not shared infra.
- **Resolved today** (infra + content, chronological): jest-native matcher type gap; theme key mismatches (129 errors); `container`-from-`render()` pattern (~45 errors, 2 files); `DocumentSigningService.test.ts` static-class rebind + `getInstance` fix in `LegalDocumentViewer.test.tsx` (partial mock-drift bucket); `logger.*()` extra-argument signature (32 call sites, 420→420 test-safe); `jest-expo` 52→54 + `@types/react` bump; 7 stale content/behavior test assertions; AsyncStorage mock wiring (12 suites) + `firebase` ESM transform gap (3 suites); `useAudioPosition.ts` missing `React` import; FCM feature removal (dead code, 1 suite); `DevMenu` invariant violation (3 suites, this item).
- **Still genuinely untriaged** (real per-file review needed, nothing guessed at):
  - `src/services/legal/__tests__/DocumentVersioningService.test.ts` — **fixed 2026-08-17**, 0/42 → 39/42, stable across 3 runs. Full rewrite against the real static `DocumentVersioningService` API (confirmed method-by-method against source, no `Firestore`/`expo-crypto` dependency — AsyncStorage-only). Mechanical mappings: `getInstance()` → rebind to the class itself (static, no singleton — same pattern as `DocumentSigningService.test.ts`); `getUserAcceptances(userId)` → `getAcceptanceHistory()` (no `userId` param — reads current-device history, not filtered per user); `isDocumentAccepted(docId, userId)` → `hasAccepted(documentId)` (1 arg); 5-arg `recordAcceptance(docId, userId, version, platform, appVersion)` → real 3-arg `recordAcceptance(documentId, appVersion, platform)` — `userId` comes from `useAuthStore` internally, `version` from `LegalDocumentService.getDocument(documentId).version` internally (mocked here, same flat `{__esModule:true, default:{...}}` pattern used elsewhere in this session); `requiresSignature(documentObject)` → `requiresSignature(documentId: string)`, string not object. Switched the fake `'test-doc-001'` ID to a real one (`'terms-of-service'`, from the bundled `src/assets/legal-documents/index.json`) since `requiresSignature`/`getVersionHistory`/`getDocumentsRequiringSignature` read the real static index directly, not AsyncStorage — an invented ID would never match. Tightened several loose `expect(Array.isArray(...)).toBe(true)`-only assertions (that passed regardless of correctness) into real value checks now that the mocks are wired correctly (e.g. `linkSignatureToAcceptance`/`hasValidSignature` now actually seed a matching acceptance record and assert `true`, not just "returns a boolean"). **User decision, 3 methods dropped** (`getDocumentVersion`, `getCurrentVersion`, `getAllDocuments`): confirmed via full method-by-method read of the real class that none of these — or anything resembling them — exists on `DocumentVersioningService`; the invented shapes (a per-doc AsyncStorage-backed catalog with `.content`/`.title` fields) belong to the separate `LegalDocumentService` class instead. Rather than guess a redirect, dropped these 3 methods' tests and every test whose premise depended solely on them (9 of the original 42 `it()` blocks total, not just 3 — cascading through `service.getDocumentVersion('')`/`getAllDocuments()`-based edge-case and performance tests too). Added a few new small tests to keep meaningful coverage of paths the drop removed (e.g. `recordAcceptance returns false when the document does not exist`, `recordAcceptance returns false with no authenticated user`) — net 39 tests, all real. Whole-`src/` grep for every real `DocumentVersioningService.*` call site found **zero production bugs** — every real caller (`LegalDocumentsScreen.tsx`, `LegalDocumentViewer.tsx`, `ComplianceReportService.ts`) already uses correct signatures; the item-40-style bug hunt came back clean this time. `tsc --noEmit`: 0 errors in this file before and after.
  - `src/services/legal/__tests__/IntegrationTests.test.ts` — **fixed 2026-08-17**, 0/21 → 21/21, stable across 3 runs. Full rewrite exercising 6 real services end-to-end (`DocumentAnalyticsService`, `DocumentSigningService`, `DocumentVersioningService`, `BackupService`, `CloudBackupService`, `ComplianceReportService`). Fixes, roughly in the order they surfaced:
    - **Import bug**: `DocumentAnalyticsService`/`BackupService`/`CloudBackupService` were named imports against default-export-only classes (resolves to `undefined`) — switched to default imports. `ComplianceReportService` is the one class here with both a real named export and a real `getInstance()` singleton — left as-is.
    - Same `DocumentVersioningService` API mapping as the unit-test fix above (`recordAcceptance` 3-arg, `hasAccepted`, `getAcceptanceHistory`).
    - `captureSignature(docId, {type:'sketch',...}, ...)` → `{type:'typed', data, timestamp, device}` (`CapturedSignature`'s real shape — `type` is a `'typed'`-only literal, sketch capture was deliberately removed from the UI, per the `DocumentSigningService.test.ts` precedent from item 9 above).
    - Added `jest.mock('expo-crypto', ...)` (module-level, deterministic `mock-hash-${data}`) and a mock for `LegalDocumentService` (fixture doc + `getAllDocuments`) — `recordAcceptance`/`createLocalBackup` both read real documents through it.
    - **Real infra gap, not just an API mismatch**: the auto-mocked `AsyncStorage` is stateless (`setItem` recorded but never reflected by a later `getItem`), which structurally cannot support what an integration test needs to prove — "write via one service, read back via another." Replaced with a `Map`-backed in-memory implementation in `beforeEach` (`getItem`/`setItem`/`removeItem`/`multiRemove`/`getAllKeys` all operate on the same real store) so cross-service consistency checks are genuinely exercised, not coincidental.
    - `LegalDocumentService.getAllDocuments` defaulting to `[]` silently zeroed out every `ComplianceReportService.generateComplianceReport()` call — its `documents[]`/`summary` are built entirely from that list (confirmed by reading the real implementation), so an empty mock meant `summary.viewCount`/`summary.acceptedDocuments`/etc. were structurally always 0 regardless of what analytics/acceptance data existed. Fixed the mock default to return `[testDocument]`.
    - Backup checksum verification (`verifyBackupIntegrity`/`restoreFromLocalBackup`): rather than the sibling `BackupServices.test.ts`'s loose `typeof result === 'boolean'` pattern (accepted precedent there since replicating the real hash algorithm is expensive), the deterministic mocked crypto here makes exact checksums computable — constructed genuinely-valid backup blobs by re-fetching the same real sources (`DocumentSigningService.getUserSignatures`, `DocumentVersioningService.getAcceptanceHistory`) the checksums were originally computed from, so these assertions are real, not coincidental. Also found and fixed a bug in my own first draft: `restoreFromLocalBackup()` calls `verifyBackupIntegrity()` internally, a **second** independent `FileSystem.readAsStringAsync` call — `mockResolvedValueOnce` only covered the first, `mockResolvedValue` fixed it.
    - `CloudBackupService` genuinely can't be exercised meaningfully here — it calls `getFirestore()` directly (not through the mocked `@/config/firebase`), no Firebase app is initialized in this test env, every call throws internally and is caught into a `null`/`void` return. Rather than leave the original's tautological `expect(x === null || x !== null).toBe(true)` assertions (flagged during investigation as not able to catch a real regression either way), asserted the actual deterministic outcome (`toBeNull()`/`toBeUndefined()`) — honest about what this test env can and can't prove for cloud sync, not a false positive.
    - Noted, not chased: importing this file triggers a pre-existing, project-wide Sentry `setInterval` leak (via `ComplianceReportService.ts` → `LoggerService.ts` → `@sentry/react-native`) that prints Jest's "did not exit" warning every run — harmless, unrelated to this fix, tests pass reliably regardless.
    `tsc --noEmit`: 0 errors in this file before and after.
  - `src/services/legal/__tests__/BackupServices.test.ts` — **fixed 2026-08-17**. Same `getInstance`-on-static-class drift as `DocumentSigningService.test.ts`: `BackupService`/`CloudBackupService`/`BackupScheduleService` are all static-only, no `getInstance`; named imports (`import { BackupService } from ...`) also failed since these are default exports, masking the real error count under a single cascading "no exported member" per file. Switched to default imports, rebound `service` to `typeof <Service>` per the established pattern, renamed the "getInstance returns singleton instance" tests to "X is a stable static reference" (trivial but harmless — a static class reference is always `=== ` itself). That surfaced real shape drift in `BackupFile`/`BackupScheduleStatus` (same invented-fields pattern as `CapturedSignature` in item 9's first bullet): tests referenced `backup.metadata`/`backup.data`/`backup.checksum` (flat string) which don't exist on the real `BackupFile` — real shape is flat `version`/`createdAt` at top level, `checksums: {documents, signatures, acceptances, metadata}` (per-category hashes, not one flat string), `contents: {documentCount, signatureCount, acceptanceCount}` in place of `data`. Also `BackupScheduleStatus.isScheduled` doesn't exist — real field is `enabled`. Remapped each assertion to the real field it was clearly trying to check (no behavior invented). `tsc --noEmit` for this file: 3 visible import errors → 11 more surfaced once imports fixed → 0. `npm test` for this file: 0/67 → **59/67 pass**. Remaining 8 failures are real per-file behavior mismatches, out of scope for this mechanical fix, left untriaged: `createLocalBackup`/`restoreFromLocalBackup` don't throw on FS errors the test expects them to reject on; `clearAllBackups` doesn't call `AsyncStorage.removeItem` the way the test expects; `checkAndRunScheduledBackup`/`cleanupOldBackups` on `CloudBackupService` return `void` but tests assert `typeof result === 'boolean'`; `getDaysUntilNextBackup` returns `-1` in this mock setup where the test expects `>= 0`; `setFrequency('invalid-frequency')` resolves instead of rejecting. `tsc --noEmit` project total: 369 → 366.
  - `src/screens/legal/__tests__/ComplianceAnalyticsScreen.test.tsx` — **fixed 2026-08-17**. Started as the same `getInstance` drift (`DocumentAnalyticsService` is static-only, no `getInstance`), but investigating it surfaced much deeper drift plus two real app bugs — full rewrite, not a mechanical rename:
    - **Real app bugs found in `src/screens/legal/ComplianceAnalyticsScreen.tsx`, fixed with the user's explicit go-ahead:**
      1. Timeline tab gate checked `state.report.timeline`, a field that doesn't exist on `ComplianceReport` (real fields are `acceptanceTimeline`/`signatureTimeline`) — always falsy, so the Timeline tab showed "No Timeline Events" regardless of real data. Fixed to check `acceptanceTimeline.length > 0 || signatureTimeline.length > 0` (line ~638).
      2. Success Rate display did `Math.round(successRate * 100)}%`, but `DocumentAnalyticsService.getSignatureStats()` already returns `successRate` on a 0–100 scale (`(successCount/attempts.length) * 100`) — real users would see e.g. "10000%" instead of "100%". Fixed to `Math.round(successRate)}%` (line ~723).
    - **Test file, full rewrite:** `ComplianceReportService`'s real instance methods are `exportReportAsJSON`/`exportReportAsCSV`/`exportReportAsPDF`/`verifyAcceptancesValid` (not the invented `exportToJSON`/`exportToCSV`/`exportToPDF`/`verifyAcceptances`), returning `ExportFormat {format, content, fileName, mimeType}` and `AcceptanceVerification {allValid, validCount, invalidCount, expiredCount, issuesFound}` respectively — not the flat shapes the old fixture invented. `DocumentAnalyticsService` methods are static (`getUserViewStats()`, `getSignatureStats()`), not `getInstance().getAnalyticsData()`. Rebuilt `mockComplianceReport` to match the real nested `ComplianceReport` shape (`summary{overallCompliance,...}`, `documents[]`, `jurisdictionalCompliance[]` — not the old flat `documentStatuses`/`overallCompliancePercentage`/`jurisdictionalStatus`). The old `jest.mock('@/services/legal/DocumentVersioningService')` + `getAcceptanceTimeline` mock was dead weight — the screen never imports that service at all (timeline data comes embedded in the compliance report). Also added two mocks the old file was missing entirely, without which nothing could ever have passed: `@/stores/useAuthStore` (the screen calls it as a hook, real default is `user: null`, so `loadComplianceData`'s `if (!user?.id) return` guard would otherwise fire every time and the screen would never leave its loading spinner) and `@react-navigation/native`'s `useFocusEffect` (throws "Couldn't find a navigation object" without a real `NavigationContainer` ancestor — stubbed to a plain mount-effect, behaviorally equivalent for this component's usage). Removed 3 tests for UI that was never built: a `getByRole('button', {name:/refresh/i})` refresh button (the screen only has RN's native `RefreshControl` pull-to-refresh, not an accessible button) and an "Engagement" metrics section on the Metrics tab (no such section exists in the JSX — `DocumentAnalyticsService.getEngagementMetrics()` exists but the screen never calls it). `tsc --noEmit` for this file: already 0 (drift wasn't type-visible, `jest.mock()` auto-mocks satisfy the type checker regardless of real shape). `npm test`: 0/43 → **38/38 pass** (43 → 38 tests after removing the 3 dead ones above; net -5 counts the 2 duplicate-assertion merges too). `tsc --noEmit` project total: 366 → 361 (the 5-error drop is from the two `src/` bug fixes above, not the test file).
  - `src/services/legal/__tests__/DocumentAnalyticsService.test.ts` — **fixed 2026-08-17**. Same `getInstance`-on-static-class drift as the last three fixes (named import + `getInstance()` — `DocumentAnalyticsService` is static-only). Rebind was the easy part; the real issue was that several methods (`getUserViewStats`, `getSignatureStats`, `getEngagementMetrics`, `getAcceptanceMetrics`) don't read back a pre-aggregated stats object — they read the RAW stored event/attempt array and compute the aggregate themselves. The old fixture fed each of them a pre-computed stats object; since a plain object isn't iterable, `for (const x of parsed)` threw inside the method, which the method's own try/catch silently swallowed into an all-zeros default — so the old assertions on the fixture's fabricated numbers only "passed" by coincidence of `getInstance` throwing first. Rebuilt each fixture as the real raw shape (view events, signature attempts, acceptance times) sized to produce the target aggregate (e.g. 15 view events across 5 docs at 3000ms each → averageViewDuration 3000). Also found `getAcceptanceMetrics` hardcodes `totalPossibleAcceptances: 1` and `acceptanceRate` to 0-or-100 (not a proportional rate) — the old test's premise (`acceptedCount: 8, totalPossibleAcceptances: 10, acceptanceRate: 0.8`) doesn't correspond to anything the real method can return; rewrote to test the real binary-rate behavior. `getSignatureStats.successRate` is already 0-100 (not 0-1) — same scale bug class as the `ComplianceAnalyticsScreen` Success Rate fix, but here it was only the test's expectation that was wrong (the service itself is correct; nothing in `src/` called this with the wrong scale). `clearAnalytics` loops `AsyncStorage.removeItem` over 5 keys, never calls `multiRemove` — same "test invents `multiRemove`, real code loops `removeItem`" pattern already seen in `DocumentSigningService.test.ts`'s note (item 9 above) and the `BackupServices.test.ts` fix. `getPendingSyncEvents`/`markSyncComplete` operate on an array of event-ID strings, not full event objects — old fixture's `getPendingSyncEvents` mock used full objects but its assertion (`Array.isArray`) was loose enough to pass anyway; tightened for clarity. `tsc --noEmit` for this file: 1 import error → 0. `npm test`: 0/38 → **39/39 pass** (added one test for the `markSyncComplete` removeItem branch that wasn't covered). `tsc --noEmit` project total: 361 → 360.
  - `src/services/legal/__tests__/ComplianceReportService.test.ts` — **fixed 2026-08-17, along with a critical real bug in `src/services/legal/ComplianceReportService.ts` itself.** Started investigating the usual `getInstance` drift and instead found `ComplianceReportService.ts` calls `DocumentVersioningService.getUserAcceptances(userId)` and `DocumentVersioningService.getAllDocuments()` at 4 call sites (`generateComplianceReport`, `getAcceptanceTimeline`, `getSignatureTimeline`, `verifyAcceptancesValid`) — **neither method exists anywhere in the codebase.** This meant the entire compliance-report feature threw on every real call in production (caught + rethrown in `generateComplianceReport`/`verifyAcceptancesValid`; caught + returns `[]` in the two timeline methods) — confirmed this is also why the `ComplianceAnalyticsScreen` fix above only ever showed real users "No Compliance Data Yet" (the screen's own tests passed because they fully mock `ComplianceReportService.getInstance()`, bypassing this internal bug entirely). Flagged to the user before touching `src/` given the legal/compliance stakes; got explicit go-ahead. Fix, in `ComplianceReportService.ts` (all 4 call sites): `DocumentVersioningService.getUserAcceptances(userId)` → `DocumentVersioningService.getAcceptanceHistory()` (real method, no `userId` param — this service is single-current-user, reads it internally via the auth store) and `DocumentVersioningService.getAllDocuments()` → `LegalDocumentService.getAllDocuments()` (new import — `DocumentVersioningService` never had this method, `LegalDocumentService` does). That surfaced two more real field-name mismatches once `DocumentAcceptance`/`LegalDocument`'s actual shapes were reachable: `doc.required`/`doc.currentVersion` → `doc.requiresAcceptance`/`doc.version` (3 sites), and `acceptance.signatureType` (referenced for the signature-timeline's sketch-vs-typed display) doesn't exist on `DocumentAcceptance` at all — hardcoded to `'typed'`, consistent with the already-documented product decision that sketch signatures were deliberately removed from the UI (see `DocumentSigningService.test.ts`'s note in item 9 above). **Test file:** the service's own `ComplianceReportService.getInstance()` binding was already correct (real singleton, both named + default exports exist) — the drift was entirely in its *dependency* mocks: `jest.mock('../DocumentVersioningService', () => ({DocumentVersioningService: {getInstance: () => ({...})}}))` invents a getInstance-singleton shape for what's actually a static-only default-exported class, so under Babel's `esModuleInterop` the real code's `DocumentVersioningService.getAcceptanceHistory` resolved against the wrong object entirely and threw. Same wrong-shape mock existed for `DocumentAnalyticsService`, and `LegalDocumentService` had no mock at all (newly required by the fix above). Rewrote all three as flat `{__esModule: true, default: {method: jest.fn()...}}`. Also found and fixed one real test-isolation bug independent of all the above: `jest.clearAllMocks()` (this file's `beforeEach`) clears mock *call history* but not queued `mockResolvedValueOnce()` *implementations* — an earlier test's unconsumed queued value was leaking into "audit trail events are marked as immutable", making it fail only when run as part of the full suite (passed in isolation). Fixed by giving that test its own `mockReset()` on `AsyncStorage.getItem` rather than relying on the shared queue. One test's expectation was also just wrong: `clearComplianceData handles errors gracefully` expected `resolves.not.toThrow()`, but the real method deliberately rethrows storage errors (unlike most methods in this file, which swallow-and-default) — a silent failure to wipe a user's data on account deletion should surface, not vanish; renamed to `clearComplianceData rethrows storage errors` and flipped to `rejects.toThrow()`. `tsc --noEmit` for `ComplianceReportService.ts`: 0 → 1 (the `signatureType` field) → 0 after the hardcode fix. `npm test`: 26/47 → **47/47 pass**, stable across 3 repeated full-suite runs (confirms the flaky test is fixed, not coincidentally passing). Verified no regressions: full `src/services/legal/` + `src/screens/legal/` run stayed at the same 93 pre-existing failures / 250 passes as before this fix (all in files already itemized above, e.g. `BackupServices.test.ts`'s known 8). `tsc --noEmit` project total: 360 → 352.
  - `src/services/notifications/__tests__/NotificationService.test.ts` — **fixed 2026-08-17.** Not a `getInstance` drift at all — a completely different architecture mismatch. The whole file mocked `@/services/firebase/config` (a module that doesn't exist) and expected `initialize()`/`updateUserPreferences()`/`markAsRead()`/`clearHistory()`/`disableDailyReminder()` to call `firestore.collection(...)`. The real `NotificationService` has **zero Firestore usage anywhere** — it's a pure in-memory (`Map`-backed) + `AsyncStorage` service (confirmed via its actual imports: only `AsyncStorage`, `LoggerService`, and the types file). It's also not a `getInstance()` singleton — a plain class with a public constructor, exported both as the class itself and as a pre-built `notificationService` instance for app code. Full rewrite: dropped all firestore mocking, added the missing `@react-native-async-storage/async-storage` mock, construct a fresh `new NotificationService()` per test (avoids the in-memory Maps bleeding state across tests). Built a `buildNotification()` helper matching the real `PushNotification` interface (`priority`/`channel` as enums, `badge`, `actions: []`, `data: {}`, `timestamp` — not the ad-hoc `{priority: 'high' as const, sentAt, scheduledFor, status}` shape the old fixture used). Fixed field names throughout to match real interfaces: `DailyReminder.nextScheduledTime` → `nextScheduledFor` (and added the missing required `title`/`userId` fields); `NotificationFilter.type`/flat `startDate`/`endDate` → `types: []`/nested `dateRange: {startDate, endDate}`; `NotificationStats.totalNotifications`/`openRate`/`engagementRate` (none of which exist) → real `totalSent`/`readRate`/`dismissRate`. Rewrote the "quiet hours" test to actually exercise that code path — the real `isInQuietHours` check uses the current system clock, not any field on the notification (the old test set `sentAt` to an 11pm date, which the deferral logic never reads at all) — used `jest.useFakeTimers().setSystemTime(...)` to pin the clock inside quiet hours, then asserted the notification landed in the schedule queue (`getQueuedCount()`) rather than immediate history, which is a real, meaningful check the old version never made. Rewrote the "Event Listeners" tests, which previously called their own `jest.fn()` callbacks directly instead of triggering delivery through the service — replaced with real `sendNotification()` calls that exercise `notifyDelivered()` for real, plus added an unsubscribe-stops-notifying case that didn't exist before. Removed 4 tests for behavior the real code cannot produce: "initialization errors", "preference update errors" (via firestore), "network errors in send", and "meaningful error messages" — `sendNotification`'s entire body is one try/catch that always resolves a boolean and never rejects to the caller, and `initialize()`/`shutdown()` only touch synchronous in-memory `Map` operations with no external failure surface; kept the one error path that's real (`sendNotification` returns `false`, doesn't throw, when `userId` is empty). `tsc --noEmit` for this file: 19 errors (18 shape mismatches + the unresolvable firebase/config import) → 0. `npm test`: suite couldn't load (0/0) → **29/29 pass**. `tsc --noEmit` project total: 352 → 333.
  - `src/services/payment/__tests__/MockPaymentService.test.ts` — **fixed 2026-08-17, plus two real bugs in `src/services/payment/MockPaymentService.ts`** (this is the dev/Expo-Go fallback payment provider, not real Apple/Google billing — flagged to the user given payment-adjacent stakes, got explicit go-ahead before touching `src/`). Bug 1, confirming the behavior question this item was already flagged with: `purchase()` never checked the product existed — `createPaymentIntent()` does (`if (!product) throw`), `purchase()` didn't, so a garbage/undefined product ID silently "succeeded" and stored a purchase record for a nonexistent product. Added the same check, reusing the lookup instead of the second, now-redundant one later in the subscription branch. (The test file's own "Error Handling" tests — `should handle invalid product IDs gracefully`/`should handle undefined product ID` — already asserted `.rejects.toThrow()` for this and needed no changes; they'd just been failing on the source gap the whole time.) Bug 2, smaller: `createPaymentIntent()`'s id is a hash of `productId + Date.now()` only (millisecond resolution) — two calls in the same millisecond produce identical ids, reproduced deterministically (not flaky) via the test `should return unique intent IDs`. Added a `Math.random()` component to the hash input. **Test file:** the real blocker to even running this suite was that `purchase()`/`validateReceipt()` simulate real network delay via genuine `setTimeout(…, 1500)`/`setTimeout(…, 500)` calls, and the `should have success rate around 95%` test calls `purchase()` **100 times sequentially** — 100 × 1.5s ≈ 2.5 minutes for one assertion, which blew past jest's 5s default per-test timeout and hung the whole suite (confirmed via a backgrounded run that had to time out). Added a `purchaseFast()` helper that wraps a single `purchase()` call in local `jest.useFakeTimers()` + `jest.advanceTimersByTimeAsync(1500)`, used at every call site except the two `Performance` tests (which specifically assert on the real delay, so they're left on real timers, untouched). That dropped total suite time from 2.5+ minutes to ~2.9 seconds. Fixing the speed then exposed two more, unrelated pre-existing gaps that real time had been masking: (a) `expo-crypto`'s `digestStringAsync` resolves to `''` (empty string) in this project's jest environment for every call, confirmed independently of anything purchase-related (`crypto.subtle` isn't available under the `node` test environment this project uses) — this file was the one payment/legal test file in the codebase that hadn't added the `jest.mock('expo-crypto', ...)` every sibling test file already has (e.g. `DocumentAnalyticsService.test.ts`); added a mock here too, returning a value derived from the input so uniqueness checks stay meaningful; (b) `purchase()`'s real 5% simulated-failure branch (`Math.random() > 0.05`) is live, undeterministic randomness across ~15 purchase call sites in this file — always a latent ~1-in-a-few chance some subscription-purchase test would randomly fail, just never surfaced when the suite took 2.5 minutes to run once. Pinned `Math.random()` in `beforeEach` to a varying-but-always-safe range (`0.5 + realRandom()*0.4`, always > 0.05) so purchases succeed deterministically, restored to real `Math.random()` inside the one test that explicitly wants the true distribution. `tsc --noEmit` for this file: 0 (unchanged — none of this was type-level; the pre-existing `updatePaymentMethod`/`IPaymentService` mismatch at `MockPaymentService.ts:317` and its 5 `PaymentServiceFactory.ts` cascade errors are unrelated, confirmed via `git stash` to predate this fix, left untouched). `npm test`: 39/47 pass, suite effectively unrunnable in CI due to the 2.5-minute hang → **47/47 pass, stable across 5+ repeated runs, ~2.9s**. `tsc --noEmit` project total: unchanged at 333 (this file had no type errors to begin with).
  - `src/stores/__tests__/useTrialStore.test.ts` — **fixed 2026-08-17, plus one real bug in `src/stores/useTrialStore.ts`.** Two independent problems, neither a `getInstance` drift (this is a Zustand store, not a class service). Bug 1, a test-file mechanical issue affecting most of the 13 failures: nearly every test did `const state = useTrialStore.getState();` once, called an action (`addPracticeMinutes`/`upgradeToBasic`/etc.), then read a field off that SAME captured `state` variable — but Zustand's `set()` replaces the internal state object with a new one, so any action that calls `set()` makes a previously-captured snapshot stale. Reading a raw field off it afterward silently returns pre-update data (some assertions passed anyway, by accident, when the stale/initial value happened to satisfy the check — e.g. `toBeLessThanOrEqual(10)` against a stale `0`). Store *methods* accessed off the same stale object were unaffected (they close over Zustand's live `get()`/`set()` internally), which is why the failure pattern looked selective. Fixed by always calling `useTrialStore.getState()` fresh immediately before reading any field that an action might have changed. Bug 2: `upgradeToBasic()`/`purchaseLifetimeAccess()`/`restorePurchase()` all auto-init `state.paymentService` via `PaymentServiceFactory.create()` if it's null — under jest, `Platform.OS` defaults to `'ios'` and `expo-constants` isn't mocked, so the factory's `isExpoGo` check is false and it falls through to a real, native-module-backed `AppleIAPService`, which fails to initialize in this environment (`"Failed to initialize payment service"`, caught, `paymentService` stays null). Fixed by injecting a lightweight mock `IPaymentService` directly into `useTrialStore.setState({paymentService: ...})` in `beforeEach`, bypassing the factory entirely — the correct level to mock at for a store unit test. **Real bug found and fixed in `src/stores/useTrialStore.ts`, with explicit go-ahead:** `cancelSubscription()` used `const { subscriptionCancellationService } = await import('@/services/subscription/SubscriptionCancellationService');` (dynamic import) — under this project's Jest config, dynamic `import()` throws `A dynamic import callback was invoked without --experimental-vm-modules` *before* Jest's mock registry is ever consulted, so no test-only mock could intercept it; confirmed via direct reproduction, not a theory. Converted to a static top-of-file import (mechanical, no behavior change — this service was always needed synchronously on cancel, not lazily/conditionally, so there was no real deferred-loading benefit being lost). Also mocked `subscriptionCancellationService.cancelSubscription()` in the test (real implementation needs a live authenticated Firebase user + network call to a Cloud Function, neither available under jest). One thing flagged but left untouched, out of scope for this fix: `upgradeToBasic()` never sets `subscriptionEndDate` (only `cancelSubscription()` does, to the cancellation timestamp) — there's no client-side way to know when a subscription is due to renew; may be intentional if renewal date is tracked server-side only, may be a real gap — not investigated further, the old test asserting on this (`should set subscription end date`) was removed rather than guessed at. `tsc --noEmit`: this file had 0 errors before and after (pure runtime drift); the 5 pre-existing `useTrialStore.ts` errors (`PaymentProvider` union widening, `CancellationRequest` shape, missing `practiceSessionCount`) are confirmed unrelated via `git stash`, unchanged, left untouched. `npm test`: 20/33 → **33/33 pass**. Checked for regressions: `subscriptionFlow.integration.test.ts` (the only other file importing either changed module) went 23/30 → 24/30 — an incidental improvement from the same static-import fix, not a regression. `tsc --noEmit` project total: unchanged at 333.
  - `src/screens/auth/__tests__/SignInScreen.test.tsx` — **fixed 2026-08-17**, 5/6 → 6/6. `getByTestID` typo (wrong casing) in one assertion — RNTL's real query is `getByTestId`. One-line fix.
  - `src/components/audio/__tests__/HighlightedReadingPlayer.e2e.test.tsx` — **fixed 2026-08-17**, 33/34 → 34/34, stable across 3 repeated runs. Not a real per-test bug — root cause was `src/hooks/useWordHighlighting.ts` calling the real `audioHighlightingService` singleton (`@/services/audio/AudioHighlightingService`) unconditionally on mount (`startHighlighting(...)`), which falls through its provider chain to a real, unmocked Firestore lookup for the test's readingId (`gospel_2025-11-11`). Neither this test file nor `AudioHighlightingService.test.ts`/`HighlightingIntegration.test.ts` (still open, see below) mock that singleton or the Firestore layer — same gap in all three. The resulting async webchannel connection outlives whichever test triggered it and later crashes with `ReferenceError: XMLHttpRequest is not defined` (no such global in the jest env); Jest attributes the crash to whatever test happens to be executing when it lands, which is why the reported "failing" test (`should render all controls`) passes cleanly in isolation (`-t` filter) — the real failure is a leaked async op, not that test's assertions. Confirmed via `HighlightedReadingPlayer.tsx` reading only from its `timingData` prop for rendering (not from the hook's live state), so replacing the singleton with a no-op `jest.mock('@/services/audio/AudioHighlightingService', ...)` in the test file changes no existing assertion. `tsc --noEmit`: 0 errors in this file before and after (test-file-only change, confirmed via `git status`/grep on the fresh error list — no `src/` touched). Project-wide `tsc --noEmit` total now reads 440 (vs. the 366 last recorded 2026-08-17 in item 9's bucket above) — this predates this fix; TODO.md already flagged the file-list as stale and due for a fresh count (see note at end of type-check section), not investigated further here, out of scope for this test fix.
  - `src/services/audio/__tests__/AudioHighlightingService.test.ts` — **fixed 2026-08-17**, 14/16 → 16/16, stable across 3 runs. Not the Firestore gap this time — both failures traced to `AudioHighlightingService.getInstance()` returning the **same singleton across every test in the file**, combined with `startHighlighting()`'s re-entry guard (`if (this.activeReadingId === options.readingId && !this.isStarting) return;`) — every test reuses `readingId: 'test_gospel'` with no `afterEach` reset, so every test after the first silently no-ops on `startHighlighting()` and inherits stale state/callbacks (wrong `currentPositionMs`, wrong `onWordChangeCallback` closure) from whichever test ran before it. Fix: added `afterEach(() => service.stopHighlighting())`, which clears `activeReadingId` so each test's `startHighlighting()` does a real fresh init. No `src/` change — the guard is correct production behavior (don't restart an already-active identical reading), tests just weren't calling the reset the service already exposes for exactly this.
  - `src/services/audio/__tests__/HighlightingIntegration.test.ts` — **fixed 2026-08-17**, 21/26 → 26/26, stable across 3 runs. Same singleton/re-entry-guard gap as `AudioHighlightingService.test.ts` above (readingId `'test_john_1_1'` reused with no reset) explained 2 of the 5 failures (`should notify on word changes`, `should emit state change on...` — see below); same `afterEach(() => service.stopHighlighting())` fix. The other 3 were a separate, real drift: `state.totalWords`/`state.isLoading` don't exist on `HighlightingState` (checked `src/types/highlighting.types.ts` — only `currentPositionMs`/`currentWordIndex`/`currentWord`/`isPlaying`/`durationMs`/`updatedAt`); real word count lives on `getMetrics().wordCount`, and there's no loading-flag concept at all (by the time `startHighlighting()`'s promise resolves, loading is inherently done). Fixed 3 tests (`should load timing data from provider`, `should cleanup resources properly`, `should allow restart after cleanup`) to assert `getMetrics().wordCount` instead of the invented field, dropped the `isLoading` assertion. The 5th (`should emit state change on initialization`) tested a premise that doesn't hold at all: `startPositionTracking()` is a documented no-op (its own comment: "Position updates are driven entirely by `updateAudioPosition()` calls... no polling interval needed") — `startHighlighting()` never emits a state change by itself, only explicit actions do (`updateAudioPosition` on word change, `pauseHighlighting`, `resumeHighlighting`, `seekToPosition`, `stopHighlighting`). Renamed to `should emit state change on a playback action` and rewrote to trigger the change via `pauseHighlighting()` after start — real, documented emission path, not invented.
  - `src/__tests__/integration/subscriptionFlow.integration.test.ts` — **fixed 2026-08-17**, 23-24/30 (flaky) → 30/30, stable across 3 runs. Three independent root causes, none of them behavior bugs:
    1. **Unmocked `expo-crypto`** (this file had zero `jest.mock` calls at all): `digestStringAsync` resolves to `''` for every call under this project's node-based jest env — every purchase's subscriptionId/transactionId came out as the same empty string. Subscriptions collided on one map key (`should have yearly expires after monthly` read the same last-written entry for both lookups — identical `expiryDate`, not just close); `.filter(Boolean)` on all-empty-string transactionIds stripped everything to `[]` (`should handle multiple concurrent purchases`: expected 3 unique ids, got 0). Added the exact `jest.mock('expo-crypto', ...)` block `MockPaymentService.test.ts` already uses (deterministic `mock-hash-${data}`, unique per input).
    2. **Live `Math.random()` in `purchase()`'s real 5% failure branch**, same latent flakiness already diagnosed and fixed in `MockPaymentService.test.ts` (item 42 above) but never applied here — this file has its own ~10 purchase call sites. Pinned into the same always-succeeds-but-varying range in `beforeEach`, same convention. Confirmed `should handle payment failure and retry` (which explicitly retries on real failure) still passes trivially since its first attempt now always succeeds.
    3. **Stale-`getState()`-snapshot pattern**, the same bug class already fixed in `useTrialStore.test.ts` (item 43 above) but present here too, in a form that broke both reads AND writes: `const store = useTrialStore.getState()` captured once, then `store.currentTier = 'basic'` (direct field mutation on that captured object, not `setState()`). Zustand's `getState()` returns the live internal object by reference with no cloning, so this mutation IS real — until the next `set()`-triggering action (`addPracticeMinutes`, etc.) replaces the internal reference and orphans the captured one. Any later read OR write through the same stale `store` variable then silently no-ops or returns pre-action data. Traced one failure exactly: `should immediately enforce daily limit after cancellation` set `store.currentTier = 'free'` on an already-orphaned snapshot (orphaned by an earlier `addPracticeMinutes` call in the same test) — the live store stayed on `'basic'` (unlimited tier), so the daily-limit assertion legitimately read `false`, not a bug in `isDailyLimitReached()` itself. Fixed 4 tests (`should immediately allow unlimited practice after upgrade`, `should immediately enforce daily limit after cancellation`, `should not enforce limit for basic tier`, `should handle session expiring mid-practice`) to use `useTrialStore.setState(...)` for writes and fresh `useTrialStore.getState()` calls for every read that follows an action. Left the same pattern untouched elsewhere in this file where it happens not to matter (no action fires between a mutation and the read that depends on it) — not a wholesale rewrite, matching how `useTrialStore.test.ts` was scoped.
    `tsc --noEmit`: 0 errors in this file before and after (test-file-only change, confirmed via fresh error grep).
  - `src/components/legal/LegalDocumentViewer.tsx` + its test — **fixed 2026-08-17**, 31/48 → 48/48, stable across 3 runs. Two real `src/` bugs (flagged and approved before touching), plus a real test-only bug newly exposed by fixing them:
    - **Real bug**: the markdown-section parser (`LegalDocumentViewer.tsx`, section-building `useMemo`) treated any `#{1,6}` heading as a real section — including the document's own top-level `# <Title>` line, a normal authoring convention, already shown separately in the header. Produced a spurious, near-empty 4th section duplicating the title and an inflated section count that silently disagreed with the document's own declared `sections` metadata. Fixed with a narrow guard: skip a leading H1 only when it's the very first heading AND its text matches `document.title` exactly — doesn't touch legitimate H1s elsewhere. Fixed 6 tests as a direct consequence (duplicate title section, section count, heading hierarchy, rapid-expansion edge case).
    - **Real a11y gap**: neither this component nor `BackupExportScreen.tsx` (below) set `accessibilityRole`/`accessibilityLabel` on any interactive element — confirmed via grep (zero hits) and against the installed RNTL's `getRole()` (role is never inferred from `TouchableOpacity`/`Switch`, only from an explicit prop). Added `accessibilityRole="button"` + labels to the close/share/clear-search buttons and `accessibilityLabel` on the search `TextInput`. Fixed 8 more tests as a side effect of a genuine accessibility improvement (screen-reader users of a legal-document flow couldn't identify these controls before).
    - **Newly exposed real test bug**: `should include document version in share message` was never reaching its own assertion before (the a11y gap blocked `getByRole` from finding the share button at all) — once fixed, it surfaced that the real share message reads `"Version: 1.0.0"`, not `"v1.0.0"` (that format is header-display-only) — the test's own premise was wrong, not the source; fixed the assertion to match real output.
    - Query-collision fixes (test-only, no `src/` behavior change): "Updated 2024-01-15" and "3 sections" each legitimately render twice (header + footer; live parsed count + document's declared metadata count) — added two `testID`s (`metadataFooter`, `sectionControlsBar`) purely for RNTL scoping, zero visual/behavioral impact, and scoped the 4 affected queries with `within(...)`. `should clear search...` queried `getByRole('button', {name: /close-circle/i})` — that's the icon component's name, not the button's now-real accessible name (`/clear search/i`).
    - `should disable accept button when loading` — renamed to `should disable accept button while signing is in progress` and redesigned: `loading=true` triggers a full-screen early-return before the accept button ever renders (dead code, noted not fixed — out of scope), so that half of `disabled={loading || signingDocument}` is unreachable via the prop. Drove the reachable half through the real signing flow (open signature modal → type name → submit, with `captureSignature` mocked to a pending promise) instead. Needed an additional fix along the way: `useAuthStore` wasn't mocked in this file at all — the real store's default `user: null` meant `handleAcceptPress`'s `requiresSignature` branch always silently took the "please sign in" `Alert` path instead of opening the modal, invisible before since the one test touching this path only asserted the button was still on screen either way. Also found RNTL's host-tree traversal doesn't reliably surface `disabled` as a prop/style on this component's nested-Text button structure — `UNSAFE_getAllByProps({disabled: true})` matches the composite `TouchableOpacity` directly instead, sidestepping it.
    `tsc --noEmit`: 0 new errors (3 pre-existing errors at `mockDocument.sections`'s fixture shape, lines 85-87, confirmed via diff to predate this fix — untouched, out of scope).
  - `src/screens/legal/BackupExportScreen.tsx` + its test — **fixed 2026-08-17**, 37/47 → 47/47, stable across 3 runs. Same already-approved a11y decision as `LegalDocumentViewer`, applied to the second file: added `accessibilityRole="button"` + distinct labels to the back button, `BackupCard`'s restore/delete buttons ("Restore backup"/"Delete backup" — the distinct labels mattered, see below), and `accessibilityLabel="Password"` on the password `TextInput`. Fixed 5 tests directly.
    Remaining 5 were genuine test-only bugs, no `src/` behavior change beyond the a11y attributes above:
    - `should render back button...` queried `getByRole('button', {name: /chevron-back/i})` — the icon component's name, not the real accessible name — fixed to `/back/i`.
    - `should toggle auto backup` used `fireEvent.press` on a `Switch` — RN's `Switch` has no `onPress`, only `onValueChange` — fixed to `fireEvent(toggle, 'valueChange', false)`.
    - `should validate password length` read `.props.disabled` off the `<Text>` label instead of the `TouchableOpacity` parent — same class as `LegalDocumentViewer`'s accept-button fix; `.parent?.props.disabled` was ALSO unreliable here (same nested-Text host-tree quirk), landed on `screen.UNSAFE_getAllByProps({disabled: true})` matching the composite element directly.
    - Two decimal-formatting mismatches (`should show backup details in card`, `should show backup information`): source does `.toFixed(2)` → real text is `"2.50 MB"`, test regex was `/2.5 MB/` (one decimal, never matched) — fixed the regex. `should show backup information` (History tab) additionally renders BOTH a Cloud Backups and Local Backups section with the same mocked size — genuinely renders "2.50 MB" twice; switched to `getAllByText(...).length === 2` instead of a single `getByText`.
    - `should confirm restore action` filtered buttons via `b.props.testID?.includes('download')` — **no `testID` exists anywhere in this component**, so the filter always returned nothing and the test's own `if (restoreButton)` guard made it silently no-op and pass vacuously regardless of real behavior. Filtered by the new `accessibilityLabel: 'Restore backup'` instead, and replaced the trailing "Implementation-specific details" comment with a real assertion (`Alert.alert` called with `'Restore Backup', ...`).
    - `should switch between tabs` asserted `screen.getByText('Backup Status')` synchronously right after `render()`, racing `loadBackupData()`'s `useEffect` — switched to `findByText`, matching every other test in the file.
    `tsc --noEmit`: 5 pre-existing errors in touched files (2 in the test file's `mockBackup`/`mockCloudBackup` fixtures, 3 in `BackupExportScreen.tsx` itself — icon-name string typing, missing `Typography.h4`), all confirmed via `git diff` to sit outside every hunk this fix touched — not chased, out of scope.
  - This closes out every file identified in this session's test-suite triage — `TODO.md`'s "Type-check debt" section items 9-54 (2026-08-15 through 2026-08-17) are now fully resolved except the pre-existing, project-wide `tsc --noEmit` error count noted throughout (never re-baselined after the jest-expo migration; next session should run a fresh count rather than trust the old per-file breakdown).
  - **Fresh `tsc --noEmit` baseline, 2026-08-17 (post test-suite triage):** 265 errors across 95 files. The old "~369, mostly legal/subscription/payment" estimate was stale — confirmed by this fresh run, which surfaces a different top-of-list entirely (untouched by anything in this session's triage, all pre-existing, not investigated further today):
    - ~~`src/services/payment/GooglePlayService.ts` — 19~~ — **investigated & deleted 2026-08-17.** All 19 errors traced to one root cause: `react-native-iap`'s API changed out from under this file (method renames — `getProducts`→`fetchProducts`; `requestSubscription`/`flushFailedPurchasesCachedAsPendingAndroid` no longer exist; purchase-acknowledgment calls now take a `string`, not `{token, developerPayload}`; the `Purchase` type split into platform-specific `PurchaseAndroid`/`PurchaseIOS` subtypes). Before touching anything, checked whether this file is actually live: `grep -rln GooglePlayService` (excluding node_modules) found it referenced **only** in commented-out imports in `PaymentServiceFactory.ts` and `payment/index.ts` — real production Android IAP goes through a completely different, separate file, `GooglePlayIAPService.ts` (2 tsc errors, healthy, confirmed live via `PaymentServiceFactory.create()`'s Android branch). Confirmed dead code, not a live payment bug. User approved deletion (same precedent as the Firebase Cloud Messaging removal, item 23 above) over updating its types or leaving it — deleted `GooglePlayService.ts` (552 lines) plus its two commented-out reference lines. `tsc --noEmit`: 265 → 246. `npm test` on `src/services/payment/` + `subscriptionFlow.integration.test.ts`: 77/77, no regressions.
    - ~~`src/screens/settings/SettingsScreen.tsx` — 16~~ — **investigated & fixed 2026-08-17.** This is the live settings screen (unlike its dead top-level namesake, removed above) — all 16 fixed as real code, no deletions. Distinct root causes:
      - Two path-alias bugs: `CacheStats` type imported from `@services/cache` (barrel doesn't export types) instead of `@/types/cache.types`; `@types/settings.types` import hit TS6137 because `@types` collides with TS's reserved DefinitelyTyped alias convention — switched to `@/types/settings.types`.
      - `CacheService` imported as the class (static-style calls) but the class has no static methods — only a `cacheService` singleton instance is exported. Switched the import and all 3 call sites (`getCacheStats`, `clearCache` — not `clearAllCache`, that method never existed). `formatBytes` is private on the class; added a local module-level `formatBytes` helper, matching the same-pattern convention already used in `DownloadManagerScreen.tsx`/`StorageQuotaService.ts`/etc. — this codebase reimplements it per-file rather than sharing one.
      - **Real bug, not just types:** `handleLanguageSelect`/`handleAccentSelect` write the reading voice's language/accent into `settings.audio.*` via `updateAudioSettings()`, but the screen's initial state read from `settings.translation.appLanguage`/`.accent` — a field that has never existed on `TranslationSettings`. Net effect: a user's saved voice language/accent silently reset to the default ('english-au'/'australian') on every screen remount, even though the selection *had* persisted correctly, just under the wrong settings key. User confirmed (AskUserQuestion) fixing the read site over papering over the types: added `appLanguage?: string`/`accent?: string` to `AudioSettings` (`src/types/settings.types.ts`, matching where the code already writes — real values like 'australian' aren't in the existing narrow `Accent` type, so left as `string` rather than misapplying that union), and changed the two init-reads to `settings.audio.*`.
      - `user?.displayName` doesn't exist on `User` (real field: `fullName`) — code already had a `|| user?.fullName` fallback in both spots, so just dropped the dead first branch.
      - `storageStats` was destructured from `useOfflineStore()` and never read anywhere in the 1800+ line file — confirmed via full-file grep, deleted the destructure and the now-unused `useOfflineStore` import. Zero behavior change.
      - Missing `accessibilityLabel` on the header back `IconButton` (required prop) — added `"Back"`, same convention as this session's earlier legal/backup a11y fixes.
      - `colors.primary.green` doesn't exist on the theme hook's dynamic palette (`primary` only has purple/blue/gradient) — swapped to the static `Colors.accent.green`, matching identical usage already elsewhere in the same file.
      - `Typography.displaySmall` doesn't exist (only `displayLarge`/`displayMedium`) — mapped the header-title style to `displayMedium`, the closest real token.
      No test file exists for this screen. `tsc --noEmit`: 218 → 202.
    - ~~`src/services/content/index.ts` — 11~~ — **investigated & whole directory deleted 2026-08-17.** Root cause wasn't just the barrel: `src/services/content/` was an entire 11-file, ~4950-line dead subsystem (`ContentDatabaseService`, `ContentService`, `SearchService`, `FavoritesService`, `ReadingsScreenService`, `NotificationsService`, `ExportImportService`, `AnalyticsService`, `SyncToCloudService`, `ValidationService`, `IntegrationService`) — an abandoned/superseded module, not a live one with a typo. Checked each file individually (not just the barrel): zero import references anywhere in the codebase, no test files. The barrel's 11 errors were a symptom (`index.ts` tried to re-export nonexistent PascalCase class names — each module only exports a lowercase singleton instance, e.g. `contentDatabaseService`), but fixing just the barrel would've left ~4900 lines of unreachable dead code with its own uncounted internal errors. User approved deleting the whole directory over a barrel-only fix. `tsc --noEmit`: 240 → 218 (the extra -12 beyond the 11 barrel errors came from errors inside the deleted files themselves, never surfaced in the top-offenders count above since only `index.ts` was bucketed).
    - ~~`src/screens/NotificationSettingsScreen.tsx` — 10~~ — **investigated & fixed 2026-08-17.** Live screen (`app/notifications/settings.tsx`), all 10 fixed as real code. Two genuinely severe live bugs, not just type drift:
      - **Save was completely broken for every user:** `handleSavePreferences` gates on `userId`, computed as `user?.uid || ''` — but `User` only has `id`, never `uid`, so `userId` was always `''`, the `if (!userId)` guard always fired, and every save attempt showed "User not authenticated" and silently did nothing. Fixed to `user?.id`.
      - **3 toggles were cosmetic no-ops:** "Daily Reminders", "Achievement Notifications", and "Performance Insights" switches read/wrote `dailyRemindersEnabled`/`achievementNotificationsEnabled`/`performanceInsightNotificationsEnabled` — none of which exist on `NotificationPreferences` (real fields: `dailyReminderEnabled`, `achievementNotifications`, `performanceInsights`). Because the write path (`handleTogglePreference(field: string, value: boolean)`) takes a loose string key, TS never caught the mismatch at the write site — only the direct `localPrefs?.X` reads got flagged, since `localPrefs` is strongly typed. Net effect: flipping these switches updated a decoy key that nothing ever read; the real preference fields were never actually changeable through this screen. Renamed all sites (including the "Reset to Defaults" handler, which had the same 3 wrong keys) to match the real field names — this is the actual fix, not just a type patch, since the field-name string IS the bug.
      - `notificationSoundEnabled` (the "Notification Sound" toggle) had no equivalent anywhere in `NotificationPreferences` — not a typo like the other 3, the feature was never wired up at all. User confirmed (AskUserQuestion): removed the toggle and its "Reset to Defaults" line rather than inventing a field with no downstream consumer.
      - `user?.uid` also appeared as its own separate tsc error at the same line as the save-guard bug above (both fixed by the same `user?.id` change).
      No test file exists for this screen. `tsc --noEmit`: 202 → 192.
    - ~~`src/services/practice/__tests__/SentenceExtractionService.test.ts` — 8~~ — **fixed 2026-08-17.** Pure test-fixture drift, no real bug: `DailyReadings` requires top-level `id`/`date` fields (in addition to `gospel`/`firstReading`/`psalm`), but 7 fixture literals across the file only built the 3 nested readings, missing both. Added `id: 'test-readings', date: new Date('2026-01-01')` to each. Also `mockReading: Reading` had a `translation: 'NIV'` field that doesn't exist anywhere on the `Reading` type (excess-property error) and was silently missing `title`/`date` (both required, but tsc's excess-property check on the same literal only surfaced the `translation` error, not the missing ones — the missing-property errors on `mockReading` itself never appeared in the list of 8, only surfaced transitively via the 7 `DailyReadings` errors) — replaced `translation` with the two real required fields. `npm test`: 24/24 pass (3x, stable). `tsc --noEmit`: 184 → 176.
    - ~~`src/services/cache/CacheService.ts` — 8~~ — **investigated & fixed 2026-08-17.** Live class (its `getCacheStats`/`clearCache` are the methods `SettingsScreen.tsx` was just wired up to, above), but the 8 errors all traced to 3 specific methods: `cachePracticeSession`, `getCachedSessions`, `getCachedSession`. Confirmed via full-tree grep: zero callers anywhere in the app. They were also internally broken regardless of callers — `cachePracticeSession`/`getCachedSessions` passed the real, camelCase `PracticeSession` type (`readingId`/`sentences`/`attempts`, from `types/practice.types.ts`) straight into `DatabaseService.savePracticeSession`/`getUserSessions`, which actually expect a different, snake_case DB-row shape (`user_id`/`reading_id`/`cached_at`) — two unrelated shapes sharing a name. `getCachedSession` was worse: it called `databaseService.getCachedReading()` (the *readings* cache table) and returned the result typed as a practice session — wrong table entirely. User confirmed (AskUserQuestion) deleting the 3 dead+broken methods over fixing them, since nothing calls them. Removed ~110 lines plus the now-unused `PracticeSession` import. `tsc --noEmit`: 192 → 184.
    - ~~`src/services/readings/ReadingService.ts` — 6~~ — **fixed 2026-08-17.** Pure type fix, no behavior change. `searchReadings()` iterated `Object.entries(readings as any)`, but TS still inferred the destructured value as `unknown` rather than `any` (the `as any` cast on the argument doesn't propagate through `Object.entries`'s overload resolution the way a cast on the result does). Cast the full expression instead: `Object.entries(readings as any) as [string, any][]`, matching the liberal `any`-casting style already used throughout this method. No test file exists; `searchReadings` itself isn't called anywhere else in the app either (confirmed via grep) but is a real public method on a live, actively-used class (`ReadingService` — see `CLAUDE.md`'s key-files table), not deletion-worthy dead code. `tsc --noEmit`: 176 → 170.
    - ~~`src/screens/subscription/RedeemGiftScreen.tsx` — 6~~ — **fixed 2026-08-17.** No real bugs, all mechanical:
      - 4 errors from one classic TS gotcha: `let errorMessage = GiftRedemptionErrors.UNKNOWN_ERROR;` had no type annotation, so TS narrowed `errorMessage` to that one string literal's type instead of widening to `string` — every later reassignment to a different error string then failed. Added `: string`.
      - Missing `accessibilityLabel` (required prop) on the header back `IconButton` — added `"Back"`. A second `IconButton` (the inline clear/validated icon next to the code input) was missing it too despite tsc not flagging that instance — added `"Code valid"`/`"Clear code"` there as well while touching this file, same a11y convention as this session's earlier fixes.
      - `size={20}` passed a raw number to `IconButtonSize`, which is `'sm' | 'md' | 'lg'` — mapped to `"sm"` (18px, the closest real size to the requested 20px; `"md"` is 24px).
      No test file exists for this screen. `tsc --noEmit`: 170 → 164.
      This closes out every file in the "top offenders" bucket from the 2026-08-17 fresh baseline — remaining debt is the long tail (~86 files, 1-5 errors each, see `/tmp/tsc-fresh.log`, not committed/regenerate if needed) plus the pre-existing 5 in `useTrialStore.ts` (item 91 above, already investigated and confirmed unrelated to anything touched this session).
    - ~~`src/screens/progress/ProgressDashboard.tsx` — 5~~ / ~~`src/screens/progress/__tests__/ProgressDashboard.test.tsx` — 5~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** One real bug, one type-only fix, one fixture-drift fix:
      - **Real bug:** header avatar initials and personalized greeting ("Welcome back! {name}'s Progress") read `user?.displayName`, a field that has never existed on `User` (real field: `fullName`, same recurring drift as `SettingsScreen.tsx`/`NotificationSettingsScreen.tsx` above). Every logged-in user always saw the generic 'U' avatar and "Your Progress" fallback instead of their name. Fixed both read sites to `user?.fullName`.
      - **Type-only:** `StreakDisplay`'s `style` prop was typed as bare `ViewStyle`, but `ProgressDashboard` (and the component's own internals, line 155) pass a style *array* (`[Shadows.lg, {...}]`) — RN supports this at runtime via `StyleProp<ViewStyle>`, the prop type just hadn't been updated to match. Widened to `StyleProp<ViewStyle>` in `src/components/progress/StreakDisplay.tsx`. No behavior change.
      - **Test fixture drift:** `mockProgressData` was missing `StreakData`'s `joinedDate`/`lastUpdated`, each `ReadingRecord` in the `readings` map was missing `date`/`timestamp`/`readingCount`/`audioUsed`/`pronunciationUsed`, and `ProgressData` itself was missing the required `stats` object while carrying two invented fields (top-level `userId`, top-level `lastUpdated` — neither exists on `ProgressData`, `lastUpdated` only exists nested inside `StreakData`). None of the added/removed fields are read by the component under test (confirmed via grep — `ProgressDashboard.tsx` never touches `progressData.stats`/`.userId`), so this is pure fixture-shape correction, not new test behavior. `npm test`: 38/38 pass (3x, stable). `tsc --noEmit`: 164 → 154.
    - ~~`src/services/offline/FirebaseSyncService.ts` — 5~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Two independent bugs, both real:
      - **Real bug, severe:** all 3 public sync methods (`syncSettingsToFirestore`, `syncProgressToFirestore`, `recordReadingToCloud`) gated on `if (!user?.uid)` — but `User` only has `id`, never `uid` (same recurring drift as `NotificationSettingsScreen.tsx`/`FirebaseSyncService.ts`'s own `verifyConnectivity` two methods below, which already correctly used `.id`). The guard was always true, so every single offline→cloud sync of settings, progress, and reading-completion data has always failed immediately with a silent "User not authenticated" error for every user — the working code right below the guard (which correctly reads `user.id`) was unreachable. Fixed all 3 to `user?.id`.
      - **Wrong error-class imports (also a latent runtime crash risk, not just a type error):** imported `FirebaseError` from `'firebase/firestore'` and `HttpsCallableError` from `'firebase/functions'` — neither is actually exported by those packages (confirmed via each package's own `.d.ts`; `firebase/firestore` internally imports its own `FirebaseError` from `@firebase/util`, and `firebase/functions` exports `FunctionsError`, not `HttpsCallableError`). Since these bad imports would resolve to `undefined` at runtime, every `error instanceof FirebaseError`/`HttpsCallableError` check in `handleSyncError`'s error-categorization logic (`isAuthError`/`isNetworkError`/`isQuotaError`/`isDataError`) was one real sync failure away from throwing `TypeError: Right-hand side of 'instanceof' is not callable` instead of gracefully categorizing the error. Fixed to the real shared base class `FirebaseError` from `@firebase/util` (which both `FirestoreError` and `FunctionsError` genuinely extend, matching the original broad-check intent) for the Firestore-side checks, and `FunctionsError` from `firebase/functions` for the Functions-side checks.
      No test file exists for this service. `tsc --noEmit`: 154 → 149.
    - ~~`src/services/payment/PaymentServiceFactory.ts` — 5~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** All 5 errors on the same line pattern (`new MockPaymentService()` at each of the factory's 5 fallback paths — Expo-Go, web-without-Stripe-config, iOS, Android, unknown-platform). Root cause traced to `MockPaymentService.ts` itself, not the factory: `updatePaymentMethod()` was typed to return `Promise<{ success: boolean; error?: string }>` instead of the real `IPaymentService` contract's `Promise<PaymentResult>` (`{success, provider, transactionId?, subscriptionId?, receipt?, error?, timestamp}` — `provider`/`timestamp` are required, not optional), and never actually populated them — every other method in this same file (`purchase`, `createPaymentIntent`) correctly builds a full result object with `provider: this.provider`/`timestamp: Date.now()`, this one method just didn't. Real (mild) bug, not just a type gap: any caller reading `result.provider`/`.timestamp` after a mock `updatePaymentMethod()` call — used whenever payments run in Expo Go, dev builds, or web without Stripe configured — got `undefined` instead of real values. Fixed to build a proper `PaymentResult` matching the rest of the file's convention. `npm test`: 47/47 pass (3x, stable). `tsc --noEmit`: 149 → 143.
    - ~~`src/services/database/DatabaseService.ts` — 5~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Two unrelated type-only fixes, no behavior change:
      - `private db: SQLite.Database | null` — `expo-sqlite`'s exported class is `SQLiteDatabase`, not `Database` (confirmed via the package's own `.d.ts`; `openDatabaseAsync()` already returns `SQLiteDatabase`, so this was a pure stale-name mismatch). Renamed.
      - `savePracticeSession`/`getUnsyncedSessions`/`getUserSessions` were typed against the app-level `PracticeSession` type (`readingId`/`sentences`/`attempts`/`startTime`/etc) — the exact same "two unrelated shapes sharing a name" mismatch already found and fixed in `CacheService.ts` above, just on the other side of that boundary. This file's actual `practice_sessions` SQLite table (and every read/write in these 3 methods) uses a completely different snake_case DB-row shape (`user_id`/`reading_id`/`recording_uri`/`result_json`/`timestamp`). Added a local `PracticeSessionRecord` interface reflecting the real row shape (matching this file's existing convention of defining its own local interfaces like `SyncQueueItem`/`CachedReading` rather than reusing app-level types) and retyped all 3 methods to it; removed the now-wrong `PracticeSession` import. `savePracticeSession` itself has zero external callers (confirmed via grep — same as the CacheService methods deleted earlier), but its sibling `markSessionSynced`/`markSessionSyncError` ARE called live from `SyncService.ts`'s practice-session sync path, so this table/subsystem isn't fully dead the way `CacheService`'s 3 methods were — left in place and fixed rather than deleted, since nothing here is confirmed unreachable end-to-end (only that nothing currently queues a `practice_session` sync action, a separate, unflagged, pre-existing gap outside this session's scope).
      No test file exists for this service. `tsc --noEmit`: 143 → 138.
    - ~~`src/screens/readings/DailyReadingsScreen.tsx` — 4~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** The main reading screen (see `CLAUDE.md`'s key-files table). Two real bugs, two mechanical:
      - **Real bug, severe:** `const { userId } = useAuthStore()` — the store only ever returns `{ user: User | null, ... }`, never a top-level `userId` (same recurring drift as `FirebaseSyncService.ts`/`ProgressDashboard.tsx` above, just destructured directly this time instead of via `.uid`). `userId` was `undefined` on every render, and the effect that records reading-view progress (`progressService.recordReading(userId, ...)`, which is what `ProgressDashboard.tsx`'s streaks/totals ultimately read — fixed two commits ago) guards on `if (!userId) return`, so it silently no-opped every time. **Reading views have never been recorded from this screen for any user.** Fixed to `const { user } = useAuthStore(); const userId = user?.id;`, and moved the declaration above the effect that uses it (tsc's 4th error here, `'userId' used before its declaration`, was flagging the exact same bug from a different angle — the old destructure was declared 45 lines below its first use).
      - **Real bug:** the offline-cached-reading banner (`<OfflineMessageBanner message="📦 You're viewing a cached reading offline" .../>`) never passed the component's required `visible` prop — `OfflineMessageBanner` uses `visible` to drive its slide-in `Animated.timing` (initializes at `translateY: -120`, i.e. off-screen, when falsy) independently of whether it's mounted, so even though the caller already gates rendering with `{showOfflineMessage && isReadingCached && (...)}`, the banner mounted permanently slid off-screen and was never actually seen. Also missing the required `title` prop (would have rendered a blank heading) and used a nonexistent `dismissDuration` prop instead of the real `autoDismissDelay`. Fixed: added `visible` + `title="Offline"`, renamed `dismissDuration` → `autoDismissDelay`.
      - Missing `accessibilityLabel` (required prop) on the header settings `IconButton` — added `"Settings"`, same convention as this session's earlier a11y fixes.
      No test file exists for this screen. `tsc --noEmit`: 138 → 134.
    - ~~`src/screens/practice/PronunciationPracticeScreen.tsx` — 4~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** No behavioral bugs this time, all 4 mechanical:
      - Two `'recordingState' used before its declaration'` errors — same TDZ shape as `DailyReadingsScreen.tsx` above, but harmless here rather than a live bug: `usePracticeStore()`'s destructure (including `recordingState`) was declared ~45 lines below two `useEffect`s that reference it in their dependency arrays. Since effect bodies only run after the full render completes (by which point the `const` is already initialized), this never actually broke at runtime — closures capture by reference — but it's still fragile/confusing ordering that tsc correctly flags. Moved the `usePracticeStore()` destructure up above both effects.
      - Missing `accessibilityLabel` (required prop) on the header back `IconButton` — added `"Back"`.
      - `colors.accent.yellow` doesn't exist on the theme's accent palette (only `green`/`greenDark`/`greenForText`/`red`/`orange` in both light and dark variants) — used for the trophy icon on the practice-complete screen. Swapped to `colors.accent.orange`, the closest real warm/gold tone.
      No test file exists for this screen. `tsc --noEmit`: 134 → 130.
    - ~~`src/services/audio/AudioPlaybackService.ts` — 4~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Type-only, no behavior change. `AVPlaybackStatus` is a discriminated union (`isLoaded: true` vs `isLoaded: false`, only the `true` branch has `isPlaying`/`positionMillis`/`durationMillis`) — the polling loop's debug `console.log` and the "stop polling" check both read those 3 fields before/without narrowing on `status.isLoaded`. Not a runtime crash (JS just returns `undefined` for the unnarrowed access, it doesn't throw), but genuinely wrong when the sound fails to load: the debug log would print `undefined` for all 3 fields instead of reflecting the real error state. Moved the log inside the existing `if (status.isLoaded)` block, and changed the stop-polling condition to `!(status.isLoaded && status.isPlaying)` so a load failure now explicitly stops polling for the right reason instead of relying on `!undefined` coincidentally being truthy.
      No test file exists for this service. `tsc --noEmit`: 130 → 126.
    - ~~`src/services/biometric/BiometricService.ts` — 4~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Live code (used by `SignInScreen.tsx`/`BiometricPrompt.tsx`/`useBiometricAuth.ts`), two real bugs:
      - **Real bug:** `supportedAuthenticationTypesAsync()` returns `AuthenticationType[]` (an array of enum values, e.g. `[1, 2]`), not a bitmask integer — but the code did `if (types & 1)`/`& 2`/`& 3` as if it were one. At runtime, bitwise-AND on an array coerces to `NaN`, and `NaN & anything` is always `0` (falsy) — so `biometricTypes` has always been reported as an empty array regardless of what the device actually supports, for every user, on every platform. Fixed to `types.includes(LocalAuthentication.AuthenticationType.FINGERPRINT)` etc.
      - **Real bug:** `authenticateAsync()` was called with a `reason` option — not a real field on `LocalAuthenticationOptions` (confirmed via the package's own `.d.ts`; the real field is `promptMessage`). Unrecognized options are silently dropped by the native module rather than erroring, so the intended prompt text ("Authenticate to access your account") has never actually been shown to any user — the Face ID/fingerprint dialog fell back to whatever generic default text iOS/Android supply. Renamed to `promptMessage`.
      No test file exists for this service. `tsc --noEmit`: 126 → 122.
    - ~~`src/services/legal/BackupService.ts` — 4~~, ~~`src/services/legal/CloudBackupService.ts` — 1 (in this bucket)~~, ~~`src/services/speech/SpeechRecognitionService.ts` — 4~~ — **investigated & fixed together 2026-08-17 (long-tail sweep), one shared root cause.** `expo-file-system`'s default (`'expo-file-system'`) export is the NEW `File`/`Directory`/`Paths` OOP API introduced in a recent SDK — the old functional API (`getInfoAsync`, `writeAsStringAsync`, `readAsStringAsync`, `deleteAsync`, `makeDirectoryAsync`, `readDirectoryAsync`, and the `DocumentDirectory`/`documentDirectory` constant) is still **type-declared** on that same default export for backwards-compat discoverability, but every one of those stub functions **throws at runtime** — confirmed via the package's own `.d.ts` doc comments ("This method will throw in runtime. ... import this method from `expo-file-system/legacy`"). All 3 files imported `* as FileSystem from 'expo-file-system'` and called these functions directly. **This was a full runtime outage, not a type error** — every local backup (`BackupService`), cloud-backup local-caching (`CloudBackupService`), and voice-recording save/delete/list (`SpeechRecognitionService.saveRecording`/`deleteRecording`/`listRecordings`) call was one line away from throwing on any real device, and most of it was invisible to `tsc` because the deprecated stub signatures still type-check fine — only the fully-removed `DocumentDirectory` constant got flagged. Fixed by switching all 3 imports to `'expo-file-system/legacy'` (the officially-signposted real implementation) and renaming `DocumentDirectory` → `documentDirectory` (the legacy module's real casing). Three smaller fixes surfaced along the way:
      - `SpeechRecognitionService.ts` separately called `await dir.exists()` on a `Directory` instance from the *new* API (still imported directly for `new Directory(...)`/`new File(...)`) — but `exists` is a synchronous boolean **property** on that class, not a method (confirmed via the native Swift source: `var exists: Bool`), so `dir.exists()` was calling a boolean as a function (`TS2349: This expression is not callable` — a real, unrelated bug this file's own type errors were flagging). Fixed both occurrences to the plain property access `dir.exists`, matching how `file.exists` was already correctly used two functions below in the same file.
      - `BackupService.ts`: `.size` access on `FileInfo` needed narrowing on `.exists` first (same discriminated-union shape as `AudioPlaybackService.ts`'s `AVPlaybackStatus` fix above) — `fileInfo.exists ? fileInfo.size : 0`, at both call sites.
      - `BackupMetadata.checksums` was typed as a loose `Record<string, string>`, which doesn't satisfy `BackupFile.checksums`'s strict `{documents, signatures, acceptances, metadata}` shape when assigned across — tightened to match, since the real object is always constructed with exactly those 4 keys.
      - **Regression caught and fixed before committing:** `BackupServices.test.ts` and `IntegrationTests.test.ts` both `jest.mock('expo-file-system')` (the old path) — switching the source imports to `/legacy` silently broke that mock targeting (Jest no longer intercepts the module the source actually imports), which surfaced as `createLocalBackup` returning `null` across most tests (real, unmocked legacy code executing in the Node test environment). Updated both test files' import + `jest.mock(...)` call to `'expo-file-system/legacy'`, and added the `getInfoAsync`/`makeDirectoryAsync` default mocks `BackupServices.test.ts` had never had (a pre-existing gap the previous, always-broken-anyway import had coincidentally never exposed). Verified via `git stash` against the true pre-fix baseline: `IntegrationTests.test.ts` 21/21 (unchanged), `BackupServices.test.ts` 59/67 pass — **exact same 8 pre-existing failures, by name**, as the untouched baseline (3x stable), confirming zero regression from either the source fix or the mock-path update.
      `tsc --noEmit`: 122 → 113.
    - ~~`src/services/speech/AzureSpeechService.ts` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Three unrelated fixes, one real bug in dead code:
      - The `crypto.getRandomValues` polyfill (needed by the Azure SDK for UUID generation) was narrowly typed/implemented for `Uint8Array` only, but the real Web Crypto signature is generic over any `ArrayBufferView` (`Int8Array`, `Uint32Array`, etc). Rewrote to operate on the raw underlying bytes (`new Uint8Array(array.buffer, array.byteOffset, array.byteLength)`) so it's correct regardless of which typed array view the Azure SDK actually passes in, not just coincidentally-Uint8Array ones.
      - `pushStream.write(audioBytes)` passed a `Uint8Array` where the Azure SDK's `write()` expects a real `ArrayBuffer` — fixed to `audioBytes.buffer as ArrayBuffer` (safe here since `loadAudioBytes` always constructs a fresh, full-length `Uint8Array`, never a subarray view with a nonzero offset).
      - **Real bug, but in dead code:** `recognizeSpeech()` called `this.loadAudioFile(...)`, a method that has never existed on this class (the real method, doing the same job, is `loadAudioBytes`) — every call would throw `TypeError: this.loadAudioFile is not a function`. `recognizeSpeech()` itself has zero callers anywhere else in the app (confirmed via grep), so this has never actually broken anything live, but it's a real typo/rename-drift bug on a public method of an actively-used class (the pronunciation-assessment path in this same file works fine) — fixed rather than deleted, matching this session's precedent for live-class-but-unused-method cases (`ReadingService.searchReadings`). Also fixed the same `Uint8Array`→`ArrayBuffer` mismatch here via `Buffer.from(audioBytes)` (Node's `Buffer` satisfies the SDK's `File | Buffer` union; already an established pattern elsewhere in this codebase — `ReceiptValidationService.ts`/`BackupService.ts`).
      No test file exists for this service. `tsc --noEmit`: 113 → 110.
    - ~~`src/services/receipt/ReceiptValidationService.ts` — 3~~ — **investigated & deleted 2026-08-17 (long-tail sweep).** Not client code that drifted — this was server-side Cloud Functions code (`firebase-functions`/`firebase-admin` imports, direct calls to Apple/Google's store-verification APIs using shared secrets) mistakenly placed inside the React Native app's `src/` tree. The 3 errors were missing modules that are correctly *not* installed client-side (`firebase-functions`, `firebase-admin`, `axios`) — installing them would have been the wrong fix, papering over a code-placement bug rather than fixing it. Confirmed dead (zero callers anywhere in `src/`/`app/`) and confirmed superseded: real, live Apple/Google receipt validation already exists in `functions/src/apple.ts`/`functions/src/google.ts` (see `CLAUDE.md`'s key-files table) — this was an orphaned duplicate, not unmigrated logic. User approved deletion (AskUserQuestion). Deleted (399 lines), no test file existed. `tsc --noEmit`: 110 → 107.
    - ~~`src/services/offline/AudioDownloadService.ts` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Live code (called from `OfflineDownloadCoordinator.ts`), severe real bug: built each reading section's download job as `{ key: 'gospel', text: reading.gospel?.text }` — but `Reading` has no `text` field (real field: `content`). `section.text` was always `undefined`, and the very next check (`if (!section.text) { failed++; completed++; continue; }`) unconditionally skipped every section of every reading as a silent "failure," for every offline audio download, ever. Fixed all 3 (`gospel`/`firstReading`/`psalm`) to `.content`.
      No test file exists for this service. `tsc --noEmit`: 107 → 104.
    - ~~`src/services/audio/HighlightingAudioService.ts` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Not the live `AudioHighlightingService.ts` (singleton actually used for word-level highlighting, see `project_highlighting_system` memory) — easily confused by name, but this is a *separate* class, and confirmed via grep to have **zero callers anywhere in `src/`/`app/`**. Left in place (fixed, not deleted — no AskUserQuestion run this time, flagging here for a future deletion pass rather than unilaterally removing a 300+ line file mid-sweep). All 3 errors were `expo-av` API drift, same family as `AudioPlaybackService.ts`/`SpeechRecognitionService.ts` above:
      - `Audio.INTERRUPTION_MODE_IOS_DO_NOT_MIX`/`Audio.INTERRUPTION_MODE_ANDROID_DO_NOT_MIX` — old constant-style names, removed from the current `expo-av`. Real replacement is the `InterruptionModeIOS.DoNotMix`/`InterruptionModeAndroid.DoNotMix` enums, imported directly. Since the old names resolved to `undefined` at runtime, `setAudioModeAsync` was silently falling back to each platform's *default* interruption mode (iOS: mix-with-others, Android: duck-others) instead of the intended do-not-mix — meaning background audio was never properly interrupted during playback through this (currently unused) service.
      - `Audio.AVPlaybackStatus` — namespaced access to a type that's actually a top-level named export (`AVPlaybackStatus` from `'expo-av'` directly, not `Audio.AVPlaybackStatus`).
      - Fixing that type properly then surfaced a real bug underneath: `status.error?.message` — `AVPlaybackStatusError.error` is a plain `string`, not an `Error` object, so `.message` always evaluated to `undefined` and the fallback `'Unknown error'` fired every time, discarding whatever real error message `expo-av` provided. Fixed to `status.error`. (Narrowing note: `if (!status.isLoaded)` failed to narrow the union here for reasons not fully run down — `if (status.isLoaded === false)` narrows correctly and is the safer form regardless.)
      `tsc --noEmit`: 104 → 101.
    - ~~`src/services/gifting/SubscriptionGiftingService.ts` — 3~~ — **investigated & whole dead cluster deleted 2026-08-17 (long-tail sweep).** Same server-code-in-client-tree pattern as `ReceiptValidationService.ts` above, but a bigger duplicate subsystem: `src/services/gifting/` had 6 files, and only `giftClientUtils.ts` (67 lines, zero imports) turned out to be real client code — actually used by `RedeemGiftScreen.tsx` (which calls the real gift-redemption Cloud Function directly via `httpsCallable`, confirmed by reading its imports). The other 5 — `SubscriptionGiftingService.ts`, `redeemGiftCodeFunction.ts`, `GiftEmailTemplates.ts`, `GiftSecurityValidator.ts`, and the `index.ts` barrel wiring them together (1908 lines total) — all import `firebase-admin` directly and call `admin.firestore()`, and confirmed via grep to have zero callers anywhere outside their own cluster (not even from `giftClientUtils.ts`, which has no imports at all). Duplicates the real, live implementation in `functions/src/gifting.ts` (see `CLAUDE.md`'s key-files table). User approved deleting all 5 (AskUserQuestion), kept `giftClientUtils.ts` untouched. No test files existed for any of the deleted files. `tsc --noEmit`: 101 → 96 (2 more than the 3 originally bucketed against this file alone — same as the `src/services/content/` deletion earlier, the extra errors were inside the other 4 files, never individually surfaced in the top-offenders count).
    - ~~`src/screens/offline/ReadingBrowserScreen.tsx` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Same `.text`/`.content` field-name bug as `AudioDownloadService.ts` above: word-count/reading-time stats read `reading.gospel?.text`/`.firstReading?.text`/`.psalm?.text`, but `Reading` has no `text` field (real field: `content`). All 3 always evaluated to `''`, so `wordCount`/`estimatedReadingTime` shown on this offline-reading-browser screen have always displayed `0` regardless of the actual reading. Fixed all 3 to `.content`.
      No test file exists for this screen. `tsc --noEmit`: 96 → 93.
    - ~~`src/screens/legal/LegalDocumentsScreen.tsx` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Three unrelated mechanical fixes, no behavior changes beyond what tsc caught:
      - `Colors.accent.yellow` (doesn't exist, same restricted palette as `PronunciationPracticeScreen.tsx`/`LegalDocumentsScreen.tsx`'s own second instance below) — swapped to `Colors.accent.orange` for the "requires signature, not yet signed" tag background.
      - `Ionicons name="storage"` — not a real Ionicons name (would render no icon). Swapped to the real `server-outline`.
      - `Colors.accent.blue` (doesn't exist — `accent` only has green/greenDark/greenForText/red/orange) inside a `|| '#2196F3'`-guarded helper — the fallback already masked this at runtime, but the property access itself was still a compile error. Swapped to the real `Colors.primary.blue`, matching the equivalent line 3 rows below (`help: Colors.primary.blue || ...`) in the same object.
      No test file exists for this screen. `tsc --noEmit`: 93 → 90.
    - ~~`src/screens/legal/BackupExportScreen.tsx` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** This closes out the final tied-at-3 file — all remaining long-tail files are now 1-2 errors each. Three type-only fixes, no behavior change:
      - `BackupInfoCard`'s `icon: string` and `Tab.icon: string` props were too loose — `Ionicons`'s `name` prop needs the real icon-name union, not arbitrary `string`. Both callers already only ever pass real valid icon names (`cloud-upload-outline`, `time-outline`, etc.), so this was pure type-looseness, not a bug. Retyped both to `keyof typeof Ionicons.glyphMap`, matching the established convention already used in `RedeemGiftScreen.tsx`/`LegalDocumentsScreen.tsx`/`OnboardingCarouselScreen.tsx`/`SuccessCelebration.tsx`.
      - `Typography.h4` doesn't exist (real heading tokens: `h1`/`h2`/`h3` only) — swapped `sectionTitle`'s style to `Typography.h3`, matching the same `sectionTitle` convention already used in `PronunciationPractice.tsx`/`AudioTranslationSetupScreen.tsx`.
      `npm test`: 47/47 pass (3x, stable). `tsc --noEmit`: 90 → 87.
    - ~~`src/screens/PronunciationPractice.tsx` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep, first pass past the tied-3 bucket).** Not `PronunciationPracticeScreen.tsx` (already fixed earlier this session) — a separate, simpler screen using `speechToTextService.analyzePronunciation` (basic Google-STT word-match comparison) rather than Azure's richer pronunciation assessment. Logged analytics via `result.comparison?.fluency`/`.completeness`/`.prosody` — but `ComparisonResult` (this service's real return shape: `accuracy`/`matchedWords`/`totalWords`/`differences`) has no such fields; only Azure's assessment produces those 3 dimensions. The `|| 0` fallback meant these were always silently logged as `0` already (no behavior change from fixing the types), so this wasn't a masked bug — just replaced the misleading optional-chain-into-nonexistent-fields with an explicit `0` and a comment explaining why, since `SessionCompletedEvent`'s analytics schema requires all 3 fields regardless of which comparison method produced the score.
      No test file exists for this screen. `tsc --noEmit`: 87 → 84.
    - ~~`src/hooks/useOptimisticBookmark.ts` — 3~~, ~~`src/hooks/useOptimisticRating.ts` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep), same root cause.** Both hooks call the shared generic `optimisticUpdate<T>()` helper (`src/utils/optimisticUtils.ts`) and pass their raw React `useState` setter (`setIsBookmarked`/`setRatingState`) directly as the `setState` argument. Since React's `Dispatch<SetStateAction<T>>` accepts `T | ((prev: T) => T)` — wider than the helper's plain `(state: T) => void` — TS's generic inference picked the wider `SetStateAction<boolean>`/`SetStateAction<RatingValue>` for the whole call's `T`, corrupting the type of every other parameter (`onSuccess`'s callback arg, etc). Fixed by wrapping each setter in a plain arrow function (`(state: boolean) => setIsBookmarked(state)`), which forces `T` to infer correctly as the real `boolean`/`RatingValue` from the other call-site arguments. Pure type fix — both hooks use a `// Mock API service (replace with actual API)` stub internally and have zero callers anywhere in the app (confirmed via grep), so this doesn't change any live behavior.
      No test files exist for either hook. `tsc --noEmit`: 84 → 78.
    - ~~`src/services/fraud/FraudDetectionService.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Real, severe bug — but currently unreachable: both imports (`../types/trial.types`, `../utils/device-fingerprint`) were one directory level too shallow for this file's actual location (`src/services/fraud/`) — they resolved to nonexistent `src/services/types/`/`src/services/utils/` instead of the real `src/types/`/`src/utils/`. This isn't just a type error; it's a hard module-resolution failure that would crash on load if this file were ever actually imported into a running bundle. Confirmed currently unreachable: its only consumer, `useFraudDetection.ts`, has zero callers anywhere in screens/components. Fixed both paths to `../../types/trial.types`/`../../utils/device-fingerprint`.
      `tsc --noEmit`: 75 → 73.
    - ~~`src/services/payment/StripePaymentService.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Real bug, but one the type checker was catching a genuine runtime need for: `createPaymentIntent()` stashes the Stripe Checkout redirect URL in `metadata: { checkoutUrl: session.url }`, and `purchase()` later reads it back via `intent.metadata?.checkoutUrl` to redirect the user to complete payment — but `PaymentIntent` (the shared cross-provider type) has no `metadata` field, so this was flying under the type checker despite being live, load-bearing data (JS doesn't strip excess object-literal properties at runtime, so it actually worked — TS just couldn't see it, and any future refactor that trusted the type would have silently broken it). Added `metadata?: { checkoutUrl?: string }` to `PaymentIntent` (`src/types/payment.types.ts`), matching the existing `clientSecret?: string; // Stripe` per-provider-optional-field convention on the same interface.
      `tsc --noEmit`: 73 → 71.
    - ~~`src/services/payment/GooglePlayIAPService.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Two real bugs in the live Android production payment path (`PaymentServiceFactory` instantiates this class directly for real, non-Expo-Go Android builds):
      - **Class didn't fully implement `IPaymentService`:** missing `cancelSubscription`/`getSubscriptionStatus`/`updatePaymentMethod` entirely — calling any of the 3 on a real Android user would throw `TypeError: ... is not a function`. Added all 3, mirroring `AppleIAPService.ts`'s established pattern exactly (Google Play, like Apple, has no programmatic subscription-cancellation API — the method explains this and points users to Play Store > Menu > Payments & subscriptions, same shape as Apple's "Settings > [Name] > Subscriptions" message; `getSubscriptionStatus` calls a `validateGoogleSubscription` Cloud Function mirroring Apple's `validateAppleSubscription`).
      - **Restored purchases lost their receipt data:** `purchase.transactionReceipt` doesn't exist on `PurchaseAndroid` (real field: `purchaseToken`, the actual Google Play verification token needed for server-side receipt validation) — every restored purchase record's `receipt` field was silently `undefined`. Fixed to `purchase.purchaseToken`.
      No test file exists for this service. `tsc --noEmit`: 71 → 69.
    - ~~`src/services/sync/SyncService.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Live code (called from `OfflineService.ts`), two real bugs:
      - `databaseService.incrementRetryCount(action.id)` was missing its required 2nd `error: string` argument — that value fills the `last_error` SQL column. `last_error` has never actually been recorded on a sync retry; the real caught error was right there in scope but never passed through. Fixed to pass `String(error)`.
      - `databaseService.getSyncMetadata?.()` called with zero args, but the method requires a `key: string` (confirmed via its own other internal call sites, which use `'last_sync'`/`'pending'`). With `key` undefined, the query never matched any row, so `getSyncStats()`'s `lastSyncTime` has always returned `null` — any UI surfacing "last synced" from this stats call has always shown never-synced regardless of real sync history. Fixed to pass `'last_sync'`, matching the key `updateSyncMetadata` itself already writes to for this same purpose.
      `tsc --noEmit`: 69 → 67.
    - ~~`src/services/receipt/ClientReceiptService.ts` — 2~~ — **investigated & deleted 2026-08-17 (long-tail sweep), along with its only consumer `useReceiptValidation.ts`.** Both errors were unfixable without a bigger rewrite: imported `react-native-store-kit-2`, a package that isn't installed anywhere in `package.json`/`node_modules` (would crash on load — not a type gap, a genuinely nonexistent dependency), and a `functions` named export that `@/config/firebase` doesn't have. Confirmed dead: `useReceiptValidation.ts` has zero callers anywhere in the app. Confirmed superseded: real, working Apple/Google receipt and purchase handling already lives in `AppleIAPService.ts`/`GooglePlayIAPService.ts` via the actually-installed `react-native-iap` (both fixed earlier this session). User approved deleting both files (AskUserQuestion). No test files existed for either. `tsc --noEmit`: 67 → 65.
    - ~~`src/services/profile/SessionCompletionService.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Genuine duplicate class member: a private `isProcessing` boolean field and a public `isProcessing(): boolean` method shared the exact same name in the same class — a real compile blocker (`TS2300: Duplicate identifier`), not a type-looseness issue. This class has zero callers anywhere in the app (confirmed via grep), likely explaining how it went unnoticed — nobody could have safely imported it while this existed. Renamed the private field to `_isProcessing`, leaving the public `isProcessing()` accessor method intact as the real external API surface.
      No test file exists for this service. `tsc --noEmit`: 65 → 63.
    - ~~`src/services/notifications/SubscriptionRenewalService.ts` — 2~~ — **investigated & deleted 2026-08-17 (long-tail sweep).** Same server-code-in-client-tree pattern as `ReceiptValidationService.ts`/the gifting cluster deleted earlier this session: 523 lines importing `firebase-functions`/`firebase-admin` directly, zero callers anywhere in the app. User approved deletion (AskUserQuestion). No test file existed. `tsc --noEmit`: 63 → 61.
    - ~~`src/services/notifications/ToastService.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Live code, two real cosmetic bugs, both now fixed and one revealing a third:
      - `duration: config.duration || this.DEFAULT_DURATION` — not a real `ToastShowParams` field (real: `visibilityTime`). Every custom toast duration (`SHORT_DURATION`/`LONG_DURATION`/caller-specified) passed by this service has always been silently dropped, falling back to `react-native-toast-message`'s own hardcoded 4000ms default regardless of what was actually requested. Fixed to `visibilityTime`.
      - `Toast.hideAll` doesn't exist on the library's `Toast` namespace (only `show`/`hide`) — calling `ToastService.hideAll()` anywhere would have thrown `TypeError: Toast.hideAll is not a function`. Fixed to `Toast.hide()`.
      - Fixing `duration` unmasked a 3rd error: a `style: { backgroundColor, borderLeftWidth, borderLeftColor }` field, also not a real `ToastShowParams` property, and (more importantly) never load-bearing even if it were — this app's root layout registers a custom `CustomToast` component via `toastConfig`/`Toast.Config()`, and that component (`src/components/notifications/CustomToast.tsx`) already computes its own per-type bg/border/text colors internally via `getToastColors(type, isDark)`, completely independent of whatever `style` object `Toast.show()` is called with. This whole block was dead, duplicate theming logic from the start. Removed it rather than trying to route it through a field that doesn't exist.
      No test file exists for this service. `tsc --noEmit`: 61 → 59.
    - ~~`src/services/notifications/DailyReminderScheduler.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Live code (wired from `SettingsScreen.tsx`'s reminder toggle via `useSettingsStore.ts`), two real bugs from an expo-notifications API version bump:
      - `setNotificationHandler`'s config used the deprecated `shouldShowAlert` alone — the current API splits that into required `shouldShowBanner`/`shouldShowList` fields (whether a notification shows as an OS banner vs. gets added to the notification list/tray while the app is foregrounded). Without them, foreground display behavior for every notification in the app (not just daily reminders — this is the app's one and only `setNotificationHandler` call) was relying on legacy-compat handling that may not reliably show a banner on current SDK versions. Added both required fields.
      - `scheduleNotificationAsync`'s `trigger: { hour, minute, repeats: true }` used the old untagged trigger shape — the current API requires a `type: SchedulableTriggerInputTypes.DAILY` discriminant on every trigger, and `DailyTriggerInput` doesn't accept `repeats` at all (a daily trigger is inherently repeating). Without the `type` tag, this object doesn't match any real trigger variant, putting the entire daily-reading-reminder notification feature at risk of being scheduled incorrectly or silently failing. Fixed to the real `DailyTriggerInput` shape.
      No test file exists for this service. `tsc --noEmit`: 59 → 57.
    - ~~`src/services/network/NetworkStatusService.ts` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Live code (feeds `OfflineIndicator.tsx`, `OfflineDownloadCoordinator.ts`, `device-fingerprint.ts`), two real bugs:
      - **Severe, confirmed live bug:** `state.type as NetworkType` blindly cast `expo-network`'s real `NetworkStateType` enum values (`"WIFI"`, `"CELLULAR"`, `"NONE"`, etc. — all uppercase) into this app's lowercase `NetworkType` union (`'wifi' | 'cellular' | ...`) without actually converting them. `networkState.type` was therefore always an uppercase string at runtime despite the type claiming lowercase. Two real downstream comparisons against the lowercase literals were consequently always wrong: `OfflineIndicator.tsx`'s wifi/cellular icon differentiation (`networkState.type === 'wifi'`) never matched, and — more seriously — `OfflineDownloadCoordinator.ts`'s "WiFi-only downloads" setting (`config.wifiOnlyEnabled && networkState.type !== 'wifi'`) was **always true** whenever that setting was enabled, since the real value was never the string `'wifi'` — meaning any user who turned on "only download over WiFi" has never been able to download anything, even while genuinely connected to WiFi. Fixed with an explicit `NETWORK_TYPE_MAP` translating each real uppercase enum value to its lowercase equivalent, instead of a blind cast.
      - `subscription.unsubscribe()` doesn't exist on `EventSubscription` (real method: `.remove()`) — calling the stored cleanup closure would throw `TypeError: subscription.unsubscribe is not a function` the first time anything actually tried to tear down the network listener. Fixed to `.remove()`.
      No test file exists for this service. `tsc --noEmit`: 57 → 55.
    - ~~`src/screens/auth/SignInScreen.tsx` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Same `StreakDisplay.tsx` style-array pattern from earlier this session: `Button`'s `style`/`textStyle` props were typed as bare `ViewStyle`/`TextStyle`, but the component's own internals already build these into style arrays (`[styles.text, getTextStyle(), textStyle]` etc), and `SignInScreen.tsx`'s two footer-link buttons pass `textStyle` as an array too. Widened both props to `StyleProp<ViewStyle>`/`StyleProp<TextStyle>` in `Button.tsx` — a shared component, so this fixes every caller that ever needs to pass a style array, not just these two call sites. No behavior change. `npm test`: 12/12 pass across `SignInScreen.test.tsx` + `Button.test.tsx` (3x, stable). `tsc --noEmit`: 55 → 52.
    - ~~`src/screens/subscription/SubscriptionScreen.tsx` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** `lifetimePrice` was destructured from `useTrialStore()` but has never existed on `TrialStoreState`, and — confirmed via grep — was never actually referenced anywhere in the component (pure dead destructure, same shape as `storageStats` in `SettingsScreen.tsx` earlier this session). Deleted it. Also added the required `accessibilityLabel="Back"` to the header `IconButton`.
      No test file exists for this screen. `tsc --noEmit`: 52 → 50.
    - ~~`src/screens/subscription/SendGiftScreen.tsx` — 2~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** One a11y fix, one real cosmetic bug: `badgeText`'s `color: Colors.white` — no top-level `Colors.white` exists (real path: `Colors.text.white`, already used correctly two other places in this same file). Badge text color has always been `undefined` at runtime, meaning the gift-tier badge text has never actually rendered white against its colored background — a real, if minor, visibility/contrast bug. Fixed to `Colors.text.white`. Also added the required `accessibilityLabel="Back"` to the header `IconButton`.
      No test file exists for this screen. `tsc --noEmit`: 50 → 48.
    - ~~`src/components/legal/__tests__/LegalDocumentViewer.test.tsx` — 3~~ — **investigated & fixed 2026-08-17 (long-tail sweep).** Pure fixture drift, missed by the earlier fix to this same file (commit `2113e8f`) because it was masked: `mockDocument.sections` was built as `{id, title, level}` objects, but the real `LegalDocument.sections` field is `string[]` (just section names) — TS reported that array-element mismatch as 3 separate errors (one per element), and in doing so never got to surfacing the OTHER problem hiding behind it: the same literal was also missing 4 required top-level fields (`filename`, `contentLength`, `category`, `requiresAcceptance`) that TS silently stopped checking for once the deeper element-type error fired. Fixed `sections` to a plain string array and added the 4 missing fields with reasonable values; the 3 other `LegalDocument`-typed fixtures further down in the file all spread `...mockDocument` so they inherited the fix automatically. `npm test`: 48/48 pass (3x, stable). `tsc --noEmit`: 78 → 75.
    - ~~`src/screens/SettingsScreen.tsx` — 6~~ — **investigated & deleted 2026-08-17.** Root cause: imports `@/hooks/useAuth`, a module that no longer exists in the codebase — a stale leftover from before an auth-hook refactor. `grep -rn "SettingsScreen"` (excluding node_modules) confirmed zero import references anywhere — the live route (`app/(tabs)/settings.tsx`) imports `@/screens/settings` → `src/screens/settings/SettingsScreen.tsx` (the 16-error entry above), a completely separate file. Confirmed dead code, not a live settings bug. User approved deletion (same precedent as `GooglePlayService.ts` above). Deleted (553 lines), no test file existed for it. `tsc --noEmit`: 246 → 240.
    - `src/stores/useTrialStore.ts` — 5 (same 5 pre-existing errors named in item 43 above — `PaymentProvider` union widening, `CancellationRequest` shape, missing `practiceSessionCount` — still unresolved, still confirmed unrelated to any store logic touched this session)
    - Remaining ~86 files with 1-5 errors each, long tail — see `/tmp/tsc-fresh.log` for the full list (not committed, regenerate with `npx tsc --noEmit` if needed)
    None of the 4 files fixed in today's test-suite triage (`DocumentVersioningService.test.ts`, `IntegrationTests.test.ts`, `LegalDocumentViewer.tsx`+test, `BackupExportScreen.tsx`+test) appear in this list — confirmed 0 type errors in all four, before and after. Next session: this is a real, substantial type-check debt pile (`GooglePlayService.ts` and the two `SettingsScreen.tsx` files alone are 41 of the 265) — worth its own triage pass rather than folding into future test-fixing sessions.

### App Store / Play Store
- [x] Android target API level 36 (Android 16) — bumped `android/gradle.properties` + `app.config.js` 2026-07-22, verified release build succeeds. Google Play deadline Aug 31 2026.
- [x] Submit new Android build/version with API36 to Play Console before Aug 31 2026 deadline — **submitted 2026-08-15**, versionCode 31 (also carries the home-screen widget + Android OTA fix, see below). Play Console had 2 unused service-account keys already granted Release-to-production; got a fresh key for `readingdaily-play-billing-982@...`, saved in repo root (gitignored), wired into `eas.json`. First submit attempt failed ("Version code 30 has already been used" — that code was burned by an earlier abandoned upload); bumped to 31, rebuilt, resubmitted, Success.
- [x] iOS v1.1.31 (build 161) — approved & released 2026-04-06
- [x] iOS v1.1.32 — approved & live on App Store 2026-04-16
- [x] iOS v1.1.33 build 163 (home-screen widget) uploaded 2026-08-14, but that version was already "Ready for Distribution" (approved, build locked, couldn't be swapped) by the time the build finished — build 163 itself was internally tagged "1.1.33" and Apple only allows attaching a build to an App Store version whose string matches exactly. Bumped `app.config.js`/`Info.plist`/`Expo.plist` to **1.1.34**, rebuilt (same build number 163 reused — Apple treats (version, build) as the unique pair, accepted fine), resubmitted, attached, filled What's New, submitted for review 2026-08-15. Approved 2026-08-15 (submission ID `934048db-6385-42a8-a126-e4a23a996b4a`), **released and live 2026-08-16**.
- [x] Android versionCode 28 (v1.1.29) — was live, not v1.1.31 as previously thought
- [x] Android versionCode 29 (v1.1.31) — approved & live on Google Play 2026-04-16
- [x] Android versionCode 31 (home-screen widget + API36 + OTA fix) — submitted 2026-08-15, Success, in Google Play review
- [x] Android layout fix (Galaxy S26) — shipped v1.1.31, verification removed
- [x] Android OTA updates were never actually enabled (`expo.modules.updates.ENABLED=false`) and had no runtime version set — fixed 2026-08-15, matches iOS now
- [x] Google Play self-declaration deadline (May 27, 2026) — passed, no action taken

### Monetisation (new model — 2026-06-02)
- [x] Replace subscription model with free app + one-time archive IAP
- [x] Reuse existing approved product `com.readingdaily.lifetime.access.v2` on both stores
- [x] Remove trial gate from readings screen (`useTrialStore` no longer blocks)
- [x] Remove TrialTimer UI from DailyReadingsScreen
- [x] Archive tab: paywall (dark hero + white card), date list (sectioned by week, mini calendar cells)
- [x] iOS IAP price set to $2.99 USD in App Store Connect
- [x] Android IAP price: $2.99 USD set in Play Console (purchase option `lifetime-access`)
- [x] Android IAP price fixed 2026-06-30 — new purchase option at $1.99 USD created in Play Console for `com.readingdaily.lifetime.access.v2`, old option deactivated.
- [x] Confirm Archive tab appears on Android after OTA — OTA pushed 2026-06-02, Archive tab restored

### Pricing
- [x] Asian market pricing updated 2026-04-06 — India, Malaysia, Philippines, Sri Lanka, Thailand, Vietnam
- Superseded — see `PRICING_GROWTH_PLAN.md` for the current $4.99 price test now running.

### Sentry Crash Reporting
- [x] Organisation slug confirmed: `ourenglishbest` — fixed in app.config.js (2026-03-26)

### App Store
### v1.1.32 — Reading Screen UI (all JS-only, OTA deployable)
- [x] Remove "Practice Pronunciation" button from reading screen — deployed 2026-04-13
- [x] Move Settings gear out of reading header → accessible via Profile tab only — deployed 2026-04-13
- [x] Combine date + liturgical label onto one line: "Mon, Apr 13 · Week 2 of Easter" — deployed 2026-04-13
- [x] Tighten header vertical padding — deployed 2026-04-13
- [x] Reduce horizontal text padding (~20px → ~12px each side) for wider reading column — deployed 2026-04-13
- NOTE: cleaner reading layout also lays groundwork for a future home screen widget
- [ ] FUTURE: Home screen widget — see notes below

### Widget (v1.1.33 or later — requires native build, not OTA)
- Home screen widget showing today's scripture snippet + liturgical season
- Small size: date + "Week 1 of Easter" label + app icon + liturgical colour accent
- Medium size: date + liturgical label + 2–3 lines of today's first reading + "Open" button
- Large size: more reading text + "Listen" button jumping directly to audio
- Liturgical colour theming carries through (Easter gold, Advent purple etc.)
- One-tap opens app directly to today's reading — reduces daily friction significantly
- Requires native code: iOS = WidgetKit (Swift), Android = AppWidget (Kotlin)
- Shared data layer needed: widget reads today's reading from same Firestore source
- Cannot be OTA deployed — needs full store build on both platforms
- Reading screen UI simplification (v1.1.32) directly informs widget layout decisions

### Next version — Metadata (must be done with next store build, cannot edit while version is live)
- [ ] Fix keyword typo: `lectionbary` → `lectionary` — greyed out while live, fix with next store build
- [ ] Replace keywords with ESL-bridging set (98 chars): `ESL,pronunciation,Catholic,Mass,liturgy,listening,language,Bible,devotion,catechism,fluency,prayer`
- [ ] Add Tagalog keywords for Philippines market: `katoliko, dasal, misa, ebanghelyo, filipinos`
- [ ] Update subtitle from "Practice Reading Scripture" → "English Practice with Scripture" (makes ESL angle explicit)
- [ ] Update Description with full rewrite (liturgical seasons, Grace, Lectio Divina framing, ESL angle)
- [ ] Update What's New: describe Project Vespers features — liturgical colours, 3-tab layout, Grace, Offered state
- [ ] New screenshots showing: Easter gold header + "Week 1 of Easter" label, 3-tab bar, Offered overlay (✦ OFFERED), Profile hub screen — 4 shots ready, upload to 6.1" slot

### Project Vespers — COMPLETE (all via OTA, no store builds)
- [x] Phase 1: Liturgical colour themes — deployed 2026-04-06
- [x] Phase 2: Tab bar simplified 5→3 (Readings · Practice · Profile) — deployed 2026-04-06
- [x] Phase 3: Grace — no streak anxiety, welcoming return state — deployed 2026-04-06
- [x] Phase 4: Offered — post-audio quiet hold, "Stay with a verse?" — deployed 2026-04-06

### Security (quarterly review due 2026-06-24)
- [x] Firestore rules — reviewed 2026-07-01: lasallian_reactions write rule tightened to field validation only
- [x] API key rotation — Google Cloud + Azure Speech rotated 2026-07-01, OTA pushed to both apps. **CORRECTION 2026-07-09:** that OTA re-bundled the *stale local `.env` key* (env.ts reads `process.env` before `Constants.expoConfig.extra`, and `EXPO_PUBLIC_*` inlining at `eas update` time uses whatever's in the local `.env` at build time, not the EAS-hosted env var) — translate/TTS broke live in production for ~a week until caught and re-shipped 2026-07-09.
- [ ] **Standing rule going forward:** before running `eas update` after any API key rotation, diff local `.env` against `eas env:list --environment production` first — update `.env` to match if it's drifted. `.env` is gitignored, so this has to be checked manually on whichever machine runs the update; it won't surface as a diff.
- [ ] MobSF scan — defer to pre-v2.0 release
- [x] Firebase Console — Firestore checked 2026-07-01: 3.5K reads/190 writes/30 deletes over 30 days, 0 denies, 0 errors. Clean.
- [x] Dependencies — `npm audit fix` run 2026-07-01 on both apps. Remaining: 15 moderate (LaSallian), 2 high + 20 moderate (ReadingDaily) — all transitive, no fix without breaking changes.
- [x] Removed live UI entry point into the dead subscription model (2026-07-09) — Settings → "Manage Subscription" still routed to the old Basic Monthly/Yearly purchase screen, threw "Purchase Failed" for any user who tapped it. Pulled the Settings row + `onManageSubscription` prop chain + orphaned `getSubscriptionDetail()` from `SettingsScreen.tsx`/`app/(tabs)/settings.tsx`. Legacy `SubscriptionScreen`/Cloud Functions left in place, unchanged.
- [x] **Found the "Purchase Failed" was lying (2026-07-09):** `com.readingdaily.basic.monthly.v2` was still Approved-for-sale in App Store Connect (group "ReadingDaily Premium" — hence "Monthly Premium" on the receipt, not a separate SKU). App showed purchase failure; Apple actually charged and confirmed the subscription. Verified zero real subscribers via App Store Connect subscriber count — only the one test purchase, no customer cleanup needed. Removed Basic Monthly/Yearly from sale in App Store Connect. Made `app/(tabs)/subscription/index.tsx` inert (shows "No longer available", no purchase code renders) as a second layer against stale deep links.
- [x] **Third entry point found — the app's actual front door (2026-07-09):** `src/screens/auth/LandingScreen.tsx` (shown to every logged-out user) had "Start 7-Day Free Trial" + "Subscribe: $1.99/month" buttons. The $1.99 was a hardcoded fallback string with no real product behind it (real fetch target was the now-removed `com.readingdaily.basic.monthly.v2`); both buttons called the same `initializeTrial()` → sign-up handler, no purchase call fires — so no billing risk, but it lied about the business model to 100% of new users. Rewrote to a single honest "Get Started" CTA + "Free to use" copy. `onDemo` prop and `demoButton` style were already dead pre-existing (unrelated, left alone).
