/**
 * ReadingDaily Scripture App
 * Entry point - renders the root layout from app/_layout.tsx
 * Expo Router automatically discovers routes in the app/ folder
 */

import RootLayout from './app/_layout';

export default RootLayout;
